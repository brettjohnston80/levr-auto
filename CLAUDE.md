# LEVR Auto — Project Context for Claude Code

This file exists so any Claude Code session (yours, your collaborator's, or a future one) has full context without re-explaining the business. Read this before starting build work.

## What this business does

LEVR Auto negotiates new-car purchases on behalf of consumer buyers, nationwide. Customer picks one make/model, pays a flat $699 fee, LEVR sources matching dealer inventory and negotiates by phone/email, offers land in a dashboard, customer accepts one. Money-back guarantee if no below-MSRP offer within 30 days.

Legal entity: **LEVR Holdings LLC** (Kansas). Public brand: **LEVR Auto**.

Full business logic lives in `LEVR-Auto-Core-Processes-v1.md` and `LEVR-Auto-Business-Plan-and-Roadmap.md` in this repo — read those for anything not covered here.

## Current state (updated 2026-08-13 — keep this current, it's the first thing read each session)

- **Live site:** levrauto.com (Next.js + Tailwind, deployed on Vercel, GitHub repo `brettjohnston80/levr-auto`)
- **Built and live:**
  - Database schema — `agents`, `customers`, `customer_searches`, `listings`, `qualifying_offers`, RLS on all five (`supabase/migrations/`)
  - Auth — Supabase Auth email/password sign-up + login (`/login`, `/signup`, `/account`), a DB trigger auto-creates the matching `customers` row on signup. Custom SMTP (ZeptoMail, distinct SMTP credentials from the API key already in use) is configured in Supabase Auth settings — Supabase's default email sender only delivers to Supabase org team members, which was silently blocking password reset and would have blocked real customer signup confirmations too. Verified with a real password reset email sent to `bjohnston@levrauto.com` and confirmed delivered to the actual inbox.
  - Password reset landing page — `/auth/reset-password` catches the incoming recovery link and lets the user set a new password (`updatePasswordFromRecovery`, `src/lib/auth-actions.ts`), then signs them out and redirects to `/login`. Root cause of why this was missing/broken: **this project's Auth email templates use the implicit flow**, not PKCE — recovery links redirect back with `access_token`/`refresh_token` in the URL hash fragment, which is client-only and never reaches the server. The existing `/auth/callback` route (server-side, expects a `?code=` query param to exchange) can never see a hash fragment, so pointing recovery links there — the natural first instinct — silently fails and dumps the user on `/login` with no session. The fix: `/auth/reset-password` is a client component that parses `window.location.hash` itself and calls `supabase.auth.setSession()` client-side to establish the session, then shows the form. Verified end-to-end: a real recovery link (via `admin.generateLink`, same token format `resetPasswordForEmail` produces) was opened in a real browser, correctly landed on the form, a new password was set, the app signed the session out and redirected to `/login` with a confirmation message, and logging back in with the new password succeeded — genuinely proving the new password works, not just that the form submits. Also confirmed a stale/missing link correctly shows an error instead of a broken form.
  - `/auth/callback` fixed with the same approach — was a server route (`route.ts`, `exchangeCodeForSession`), now a client page (`page.tsx`) that parses the hash fragment and calls `setSession()`, same as reset-password. This one was directly confirmed broken, not just suspected: a **genuine signup** through the real `/signup` UI (not the admin-API bypass every earlier test used) sent a real confirmation email via ZeptoMail, and clicking the equivalent real confirmation link landed on `/login?error=Could not verify your email` with the tokens visibly stuck in the URL hash — while Supabase's own side had already marked the email confirmed. So the account was actually usable, but the app told the user it wasn't and dropped their session, on every real signup. After the fix, a second genuine signup + real confirmation link landed cleanly on `/account`, fully signed in. If any other flow ever points `emailRedirectTo`/`redirectTo` at `/auth/callback` or `/auth/reset-password`, it'll work the same way — both now expect hash-fragment tokens, matching this project's actual email template configuration.
  - `/forgot-password` — was missing entirely (no way to trigger a reset email from the UI, only tested via direct `resetPasswordForEmail` calls/scripts so far). Simple email-only form (`requestPasswordReset`, `src/lib/auth-actions.ts`) that calls `resetPasswordForEmail` with `redirectTo` pointed at `/auth/reset-password`, and always shows the same generic "if an account exists…" message regardless of whether the email is real, to avoid account enumeration. Linked from `/login` via a "Forgot password?" link next to the password field. `/login` also gained `message` query param rendering (previously only `error` rendered, which would have silently swallowed this flow's own success message). Verified with the real form end-to-end, not scripted: submitted a real email on the real page, triggered a real `resetPasswordForEmail` call, followed the resulting link through `/auth/reset-password`, set a new password, and logged in with it successfully — the complete real user path with no admin-API shortcuts.
  - `/auth/confirm` — new intermediate landing page, added after real (post-Site-URL-fix) password reset links started showing "invalid or expired" for Brett despite the mechanism itself testing clean. Diagnosed by isolating variables: a token generated and clicked immediately (bypassing email) worked every time; a token sent through real email and clicked by Brett failed every time. Root cause: Supabase's own `/auth/v1/verify` endpoint consumes a single-use token on its very first `GET` — so *any* automated fetch of the link (an inbox security scanner pre-checking URLs before delivery, a link-preview generator, anything without a real person behind it) silently burns the token before the real user ever clicks it. This isn't specific to Zoho/ZeptoMail or this inbox — any customer's own mail security could do the same thing, so it needed a fix that doesn't depend on the sending or receiving mail environment. Fix: Supabase's email templates now point at `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=recovery` (or `type=signup`) instead of `{{ .ConfirmationURL }}` — a page on our own domain that renders inert (token sitting unused in the URL) until an explicit button click calls `verifyOtp({ token_hash, type })`. A mere fetch of the URL only loads a static page; nothing about that consumes the token, since verifyOtp only ever fires from the click handler. `/auth/reset-password` now checks for an existing session first (the case once `/auth/confirm` hands off) before falling back to its original hash-fragment parsing, which still works for direct Supabase links. Verified rigorously: simulated the exact failure mode with a plain `curl` GET of the confirm URL (no JS execution, mimicking a scanner) confirmed the page still worked afterward for a real click — proving the fetch alone doesn't consume anything — then completed the full loop (click → password set → login with new password) on production for both `type=recovery` and `type=signup`, plus confirmed a link with no token shows a clean invalid-link state. The Supabase email template change itself is a dashboard edit outside API access available in this session — Brett applied it directly.
  - One more real bug found immediately after the template edit: the actual first real reset email 404'd. The resolved link was `https://levrauto.com/**/auth/confirm?...` — a literal `/**` before the path. Cause: Supabase's **Site URL** field (not the email template, which was correct) had been set to `https://levrauto.com/**` — the wildcard syntax belongs in the separate Redirect URLs allow-list, not Site URL, which must be the bare domain. Diagnosed from the actual resolved email link (not the template source, which looks identical either way) — Brett copied the real `href` out of the received email, which is what surfaced the extra `/**`. Fixed by correcting Site URL to plain `https://levrauto.com`. Confirmed fixed with a brand-new real email after the correction, not a manually-patched URL.
  - **Both flows now verified end-to-end under genuinely real conditions** — real signup and real password reset, through real emails, clicked by Brett on his own real inboxes (`bjohnston@levrauto.com` for reset, `brettjohnston80@yahoo.com` for signup, chosen specifically to test a real address outside the Zoho/ZeptoMail ecosystem this was all triggered by), landing correctly on `/auth/confirm` → real click → real session → real password set / real account signed in. This is the strongest verification standard used all night — no `admin.generateLink` shortcuts, no bypassing email delivery. Test signup account deleted after.
  - Intake flow → real DB — the homepage intake filter writes a real `customer_searches` row. **2026-08-13: now collects only make/model/zip** — trim, color, and required-options selection moved out of intake entirely, into the post-payment finalization flow (see the finalization-flow entry below). Not signed in? An inline modal prompts login/signup right at the "Continue" click (not before) — no separate nav entry point exists for this on purpose. The row lands at `search_status = 'awaiting_finalization'` (the column default) / `guarantee_status = 'pending'` / `paid_at = null` by default — `awaiting_finalization` covers both "not yet paid" and "paid, not yet finalized"; `/finalize/[searchId]` itself gates on `paid_at` being set.
  - Payment — Stripe Checkout (test mode), flat $699. After saving the intake row, "Proceed to Payment" creates a Checkout Session (via `createCheckoutSession`, `src/lib/payment-actions.ts`) and redirects to Stripe's hosted page. On `checkout.session.completed` (`src/app/api/stripe/webhook/route.ts`), the webhook sets `paid_at` and `stripe_checkout_session_id` on the paid `customer_searches` row. `paid_at` starts the 24h refinement window, not the Day-30/Day-60 guarantee clock directly — see the solidify job below for that anchor. Verified end-to-end with a real test-mode payment and a real webhook delivery (via `stripe listen`), including a replay test proving the `paid_at IS NULL` guard makes it idempotent. Deliberately does **not** touch `search_status` — that only moves off `'awaiting_finalization'` once the customer (or an agent, on a call) explicitly finalizes trim/color/options (**2026-08-13**: see the finalization-flow entry below — this replaced an earlier design where the 24h window started automatically at payment). **2026-08-13**: the webhook also now kicks off a real, awaited `syncListingsForMakeModel` call for the paid make/model right after setting `paid_at`, so by the time the customer reaches `/finalize` there's real trim/price data from actual current inventory instead of an empty list — a sync failure here is logged but doesn't fail the webhook or block payment confirmation. Live keys not configured anywhere — test mode only until the business is actually ready to take real money.
  - **Pricing simplified to flat $699 (2026-08-12)** — eliminated the original 3-tier structure ($699/$899/$999 for 1/2/3 concurrent makes/models). LEVR now searches exactly one vehicle per engagement, always, for a flat $699. `package_size` was dropped from `customer_searches` entirely (migration `20260812130000_remove_package_size.sql`, which also redefines `switch_customer_search` without it — see the switching bullet above). `TIER_PRICING` in `src/lib/vehicle-data.ts` became a single `FLAT_PRICE` constant. The intake flow (`src/lib/intake-actions.ts`, `src/components/intake-filter.tsx`) now saves/returns a single search, not an array — the "Add another vehicle" UI and 1/2/3-tier pill selector are gone. `createCheckoutSession` (`src/lib/payment-actions.ts`) and the Stripe webhook now pass/read a single `customer_search_id` in Checkout Session metadata instead of a JSON array of ids. Multi-vehicle concurrent search is no longer a thing this app supports — a customer who wants a different vehicle uses the agent-mediated switch flow instead (see above). Also updated: `src/app/faq/page.tsx` (the "more than one car" answer no longer describes tiers), and both business docs (`LEVR-Auto-Core-Processes-v1.md` §1a/§1b/§2/§2.3, `LEVR-Auto-Business-Plan-and-Roadmap.md`) to drop the tier table and reflect flat $699/one vehicle.
    - Verified end-to-end against real (temporary) data: the migration was applied and confirmed (`package_size` column genuinely gone — a direct select on it errors). In a real browser: the homepage intake flow rendered the single-vehicle form with a "Flat $699 — one vehicle, always" badge and no tier UI; a real test customer completed intake and a real Stripe test-mode checkout (`4242...` card) showing the correct line item ("LEVR Auto — Vehicle Search", $699.00, "Honda CR-V"); a real webhook delivery (via `stripe listen`) set `paid_at` and `stripe_checkout_session_id` correctly on the single search row. Separately, a real test agent used the switch UI in `/internal/outreach` against the redefined `switch_customer_search` function — confirmed in the DB that the old row flipped to `search_status = 'switched'` with a correctly-defaulted new row (`paid_at: null`, `guarantee_status: 'pending'`), proving the RPC redefinition didn't break switching. Test data cleaned up after.
  - **Payment split from finalization (2026-08-13)** — trim/color/required-options selection moved from intake to a new explicit post-payment step, per the plan scoped in `LEVR-Auto-Business-Plan-and-Roadmap.md` ("Full flow, resolved Aug 12, 2026"). Migration `20260813180000_finalization_flow.sql` adds `'awaiting_finalization'` to the `search_status` check constraint (now the column default), plus `finalized_at`/`call_requested_at` timestamptz columns. A paid, not-yet-finalized row sits at `awaiting_finalization`; finalizing flips it to `pending_refinement` (repurposed — previously meant "created, awaiting payment," now means "finalized, in the 24h self-edit window") and stamps `finalized_at`, which is what the self-edit window and the solidify cron now anchor to instead of `paid_at`.
    - `/finalize/[searchId]` (`src/app/finalize/[searchId]/page.tsx`): auth-gated, ownership-checked, redirects to `/login` if signed out, `notFound()` if the row doesn't belong to the user, `/` if unpaid, `/account` if already finalized. Offers a choice (`FinalizeChoice`): finalize now, or request a call.
    - **Self-service finalization** (`FinalizeSelfService`, `src/components/finalize-self-service.tsx`) is the "in-depth, built off the initial Matchmaker tool" flow the business plan called for — same step-at-a-time interaction pattern as `components/matchmaker.tsx` (trim → color → options → review), but scoped to the make/model already paid for. Trim options come from real synced `listings` rows for that make/model (`buildTrimOptions`, `src/lib/finalize-trims.ts`), not mock data, with live counts and price ranges. Submits via `finalizeSelfService` (`src/lib/finalize-actions.ts`), guarded on `search_status = 'awaiting_finalization'`.
    - **Call path is deliberately manual for now** (per the "manual for now, later build into a calendar app" decision) — `requestFinalizationCall` just stamps `call_requested_at`; no real scheduling integration exists yet. These surface in a new "Finalization calls requested" section on `/internal/outreach` (`getFinalizationQueue`, `src/lib/outreach-queue.ts`), each with an `AgentFinalizeSearchForm` (`src/components/agent-finalize-search-form.tsx`) that submits via `finalizeSearchByAgent` (`src/lib/outreach-actions.ts`) — the agent-authorized counterpart to `finalizeSelfService`, same effect, gated by `getAuthorizedAgent()` instead of customer ownership (same pattern as the existing agent-only search-switching flow).
    - **Self-edit within the 24h window** (`updateFinalizedSearch`, same file) writes the same trim/colors/required_options fields but deliberately does **not** touch `finalized_at` — the window doesn't reset on edit, so a customer 12 hours in who makes a change stays at 12 hours remaining, not back to 24. Surfaced on `/account` via `FinalizeEditForm` (`src/components/finalize-edit-form.tsx`): a live countdown (client-side, ticks every minute, computed from `finalizedAt + 24h`) plus a collapsed summary that expands into an edit form. A paid, not-yet-finalized row instead shows a "Finalize this search" link into `/finalize/[searchId]`, or — if a call was already requested — a short "an agent will reach out" message.
    - `/payment/success` no longer shows a static "payment received" screen — it now redirects straight into `/finalize/[searchId]` using `session.metadata?.customer_search_id` from the Stripe session, once payment is confirmed as belonging to the signed-in user.
    - `getCustomerDashboard` (`src/lib/customer-dashboard.ts`) and `DashboardSearch` gained `finalizedAt`, `solidifiedAt`, `callRequestedAt`, and `requiredOptions` so `/account` has what it needs to render the states above.
    - **Verified against real production data via direct SQL matching the exact server-action logic**, not a full browser click-through — driving an authenticated customer session isn't something this assistant can do without entering credentials itself, which is off-limits. A real temporary `customer_searches` row (tied to a real existing customer) was inserted at `paid_at = now()`, confirmed to correctly default to `awaiting_finalization`; the `finalizeSelfService` write was replayed manually and confirmed it flips to `pending_refinement` with `finalized_at` set and trim/colors/options written; the `updateFinalizedSearch` write was replayed and confirmed `finalized_at` stayed byte-identical while trim/colors/options changed again; `finalized_at` was backdated 25h and the solidify cron's exact due-query was run directly, confirming it matched the row, then the `solidifySearch` write was replayed and confirmed the row flipped to `searching` with `solidified_at` set. Test row deleted after. A stale doc comment on the solidify cron route (still describing the old `paid_at` anchor) was also caught and fixed in the same pass.

  - `/matchmaker` page — still front-end only, mock data, not wired to the DB
  - MarketCheck sync, core (single make/model) — `syncListingsForMakeModel` (`src/lib/marketcheck-sync.ts`) calls the MarketCheck active-listings search (`src/lib/marketcheck.ts`, always with an explicit `car_type=new`) and upserts into `listings` on `vin`, paginated (50/page, capped at 3 pages per call for now — MarketCheck free tier is 500 calls/mo and a popular model can have 10k+ national listings). `car_type=new` alone isn't fully reliable (confirmed against real data — MarketCheck still lets through older model years, e.g. a 2019 model tagged `car_type: new`), so there's a second trust check: a listing is only upserted if its model year is the current year or one year ahead (`isTrustworthyNewListingYear`); anything older or missing a year is excluded and counted in the sync response as `excludedForYear`. Manually triggerable via `POST /api/internal/sync-listings` (`{ make, model }` body) for ad-hoc single-model syncs/backfills.
  - MarketCheck sync, demand-driven scheduling — `src/lib/marketcheck-scheduler.ts`: `getNightlyMakeModels()` (distinct make/model from `customer_searches` where `search_status = 'searching'`) and `getWeeklyMakeModels()` (distinct make/model already known via `listings`, minus whatever's in the nightly set). `runBatchSync()` syncs a list of make/models sequentially, continuing past individual failures. Two Vercel Cron targets (`vercel.json`): `/api/cron/sync-listings-nightly` (`0 6 * * *`) and `/api/cron/sync-listings-weekly` (`0 7 * * 0`), both GET, both authenticated via `Authorization: Bearer $CRON_SECRET` (Vercel sends this automatically on cron-triggered requests once `CRON_SECRET` is a project env var — now set on Vercel Production and Preview). The "monthly for consistently low-movement makes/models" tier from the original strategy is deliberately not built yet — needs real usage data to define "low-movement," which doesn't exist with zero real customers. Verified end-to-end against real data: nightly correctly empty with no active searches, weekly correctly synced the two make/models already in `listings` (Civic, Camry), and — using a real temporary test customer/search — nightly correctly picked up a newly-`searching` make/model (Ford Mustang) while weekly correctly excluded it (diffed against the just-updated nightly set). Test data cleaned up after.
  - All authenticated routes reject requests with a missing/wrong `CRON_SECRET` (verified: 401).
  - Outreach queue (manual work tool, not automation) — `/internal/outreach`, gated by `requireAgent()` (`src/lib/agent-auth.ts`): reuses the existing customer-facing Supabase Auth session (no separate agent login system), a signed-in user only counts as staff if their email matches an active row in `agents`. Brett's real agent login now exists (`bjohnston@levrauto.com`, linked to the pre-existing `agents` row) — note signing in via the normal `/login` form also auto-creates a `customers` row for that email via the existing trigger, a harmless accepted side effect of reusing customer auth. The page (`src/lib/outreach-queue.ts`) lists `customer_searches` at `search_status = 'searching'`, each with dealers pulled from `listings` by exact make/model match (aggregated to distinct dealers, sorted by inventory count — no zip/radius filtering) and any `qualifying_offers` already logged. An inline expandable form per search (`src/components/log-offer-form.tsx`) logs a real dealer offer via the `logQualifyingOffer` Server Action (`src/lib/outreach-actions.ts`), which re-checks agent auth server-side and writes through the admin client (mirrors the MarketCheck sync writes — `qualifying_offers` has no client-facing insert policy). Deliberately leaves `delivered_at` and `status` at their defaults (`null`/`'pending'`) — those belong to the customer dashboard/24h-response-window flow, not built yet. No automated calling/emailing — purely a human work queue. Verified end-to-end via real browser login as Brett's actual agent account: real matching dealers rendered correctly for a temporary test search, a real below-MSRP offer was logged through the actual form and confirmed correct in the DB (`is_below_msrp` computed `true`), and all three auth-gate cases were confirmed (unauthenticated → `/login`, signed-in-non-agent → `/`, signed-in-agent → the page). Test data cleaned up after; Brett's own login's temporary test password was rotated to an unknown value — use "Forgot password" to set a real one.
  - Customer dashboard — built into `/account` in place (replaced the old bare "you're signed in" debug view; no redirect targets needed to change since login/signup already send users there). Lists all of the signed-in customer's `customer_searches` (not just `search_status = 'searching'` — a `pending_refinement` search shows explanatory copy about the 24h window instead of disappearing), each with its `qualifying_offers` (dealer name, price, MSRP, a below-MSRP badge, status, delivery time). Deliberately doesn't show `dealer_contact` or `notes` — internal-only fields, not written for customer eyes. The real logic: `getCustomerDashboard` (`src/lib/customer-dashboard.ts`) sets `delivered_at` on any offer that doesn't have it yet, at the moment it's loaded here — per the schema's own comment, `delivered_at` is the dashboard-appearance moment, not raw dealer receipt, and it's what starts the guarantee's 24h response-window clock. The write is guarded by `WHERE delivered_at IS NULL`, so it's naturally idempotent — revisiting the page, or a concurrent load, can't re-fire or double-set it. Verified with real data: a temporary customer with a `searching` search and an offer at `delivered_at = null` — first page load rendered it correctly and set `delivered_at` for real in the DB (confirmed via direct query); reloading the page left `delivered_at`/`updated_at` byte-identical, proving the idempotency guard. Test data cleaned up after.
  - Change-request logic — three pieces, all in `/account` (customer-facing) and `/internal/outreach` (agent-facing):
    - **Accept/decline** — `respondToOffer` (`src/lib/offer-response-actions.ts`), buttons rendered by `OfferResponseButtons` on any offer at `status = 'pending'`. Sets `customer_responded_at` and `status` (`customer_accepted`/`customer_declined`), guarded by a `.eq("status", "pending")` write condition (not just a pre-check) so a double-click or race can't overwrite an already-recorded response.
    - **Sold-before-response guarantee edge case** — `evaluateOfferGuaranteeContribution` (`src/lib/guarantee.ts`) is a pure, read-time derivation (deliberately not a stored column, per the schema comment on `qualifying_offers`): below-MSRP + delivered + never sold → `'counts'`; sold and the customer responded inside the 24h window from `delivered_at` → `'does_not_count'`; sold and the customer did not respond inside that window → `'counts'`. `vehicle_sold_at` itself is set by an agent via `markOfferVehicleSold` (`src/lib/outreach-actions.ts`), surfaced as a "Mark vehicle sold" button in `/internal/outreach` on below-MSRP offers only, guarded by `.is("vehicle_sold_at", null)`.
    - **Switching make/model — agent-only, not customer self-service.** `switchCustomerSearch` (`src/lib/switch-actions.ts`) requires `getAuthorizedAgent()` (the same gate as every other `/internal/outreach` action) and calls a Postgres function, `switch_customer_search` (`supabase/migrations/20260810120000_switch_customer_search_rpc.sql`), that inserts the new row and marks the old one (`superseded_by_id`, `search_status = 'switched'`) atomically in one transaction, row-locked (`for update`) so two concurrent switch attempts on the same search can't both succeed — never a silent edit. Trim/colors/required_options reset to blank on the new row (they're model-specific); `zip` carries over; `paid_at`/`solidified_at`/`guarantee_status` reset to defaults, so the new row starts as a fresh, unpaid engagement. (`package_size` no longer exists on this table — removed along with the 3-tier pricing structure, see the pricing entry below.) Deliberately does **not** implement the $100 switch fee or 5-day free-grace-period check from `LEVR-Auto-Core-Processes-v1.md` §1b — out of scope for this pass, flagged explicitly rather than silently skipped.
      - **This changed from customer self-service to agent-only** — the original `SwitchSearchForm` (rendered on `/account`, gated on the *customer's own* session) was removed entirely and deleted from the codebase; `switchCustomerSearch` was rewired from a customer-ownership check to an agent-auth check. The only way to switch a search now is `AgentSwitchSearchForm` (`src/components/agent-switch-search-form.tsx`) on `/internal/outreach`, one per active (`search_status = 'searching'`) search card — an agent runs it after the customer requests a change by phone/email. A switched or not-yet-searching search naturally has no switch control on either page: it drops out of the outreach queue's `search_status = 'searching'` filter, and `/account` never rendered a control for any status besides the switch UI that no longer exists.
      - In its place, `/account` has a new **`AccountFaqSection`** (`src/components/account-faq-section.tsx`) — a collapsible "Need to change your search?" panel, customer-account-only (not the public `/faq` page), explaining that switching is agent-handled, with a `mailto:support@levrauto.com` link (subject + body pre-filled, including the customer's account email) that opens the customer's own mail client. `support@levrauto.com` is a real, working Zoho alias (set up outside the codebase during initial email setup, delivers into the same mailbox as `bjohnston@levrauto.com`) — confirmed with a real test email, not a placeholder.
    - Verified end-to-end in a real browser against real (temporary) data: logged in as a real test customer via a real Supabase session (magic-link token consumed through the real `/auth/confirm` click-through, same mechanism as production auth), clicked Accept on one offer and Decline on another and confirmed both persisted correctly with the right timestamp. Separately logged in as a real test agent via `/internal/outreach`, clicked "Mark vehicle sold" on two different offers, and ran `evaluateOfferGuaranteeContribution` against the real resulting DB rows: an offer delivered 30h earlier with no response, then sold, correctly returned `'counts'`; an offer accepted within a minute of delivery, then sold, correctly returned `'does_not_count'`. Test data (2 auth users, 2 `customer_searches` rows, 3 `qualifying_offers`, 1 `agents` row) cleaned up after.
    - **Switch-gate re-verified separately** (2026-08-12), after moving switching to agent-only: a real test customer's `/account` page showed no switch control anywhere, and the new FAQ section rendered and expanded correctly with a working `mailto:` link. A real test agent then used `AgentSwitchSearchForm` on `/internal/outreach` to switch that customer's Honda Accord search to a Toyota Camry — confirmed directly in the DB that the old row flipped to `search_status = 'switched'` with `superseded_by_id` pointing at a real new row carrying the correct fresh-engagement defaults (`paid_at: null`, `guarantee_status: 'pending'`, `search_status: 'pending_refinement'`). Reloading `/account` as the customer showed both rows correctly (old marked "Superseded by a newer search", new as pending refinement), still with no switch control on either. Test data cleaned up after.
  - Auto-solidify job (2026-08-12) — `/api/cron/solidify-pending-searches` (`src/lib/search-solidification.ts`), same Vercel Cron + `Authorization: Bearer $CRON_SECRET` pattern as the other cron jobs, but **hourly** (`vercel.json`, `0 * * * *`) rather than daily — 24h is a short, customer-facing-promised window, and a daily cadence could let a search sit unsolidified for nearly 48h in the worst case. No external API cost here, so there's no resource reason to prefer a coarser cadence the way there was for MarketCheck. `getSearchesDueForSolidification` finds every row still `search_status = 'pending_refinement'` with `paid_at <= now() - 24h` — same *at-least*-elapsed, catch-up-tolerant pattern as the Day-30 job below. `solidifySearch` sets `solidified_at` and flips `search_status` to `'searching'` together in one write (they describe the same event — "search doesn't start until solidification," Core-Processes-v1.md §2), guarded by `.eq("search_status", "pending_refinement")` so a concurrent run can't double-process a row. **This closes a real gap, not a cosmetic one:** before this job existed, nothing anywhere ever set `solidified_at` — it was a schema column with zero writers. Building it was a prerequisite for the Day-30 job below, which now depends on it.
    - **Anchor changed from `paid_at` to `finalized_at` (2026-08-13)** — see the finalization-flow entry below. `getSearchesDueForSolidification` now finds rows still `'pending_refinement'` with `finalized_at <= now() - 24h`, matching the new explicit-finalization design instead of the old passive payment-triggered timer.
  - Day-30 guarantee assessment job — `/api/cron/day30-guarantee-assessment`, same Vercel Cron + `Authorization: Bearer $CRON_SECRET` pattern as the MarketCheck sync jobs (`vercel.json`, daily at `0 8 * * *`). `getDueSearches` (`src/lib/guarantee-assessment.ts`) finds every `customer_searches` row with `guarantee_status = 'pending'` and `solidified_at <= now() - 30 days` — deliberately an *at-least*-30-days-elapsed check, not an exact-Day-30-only check, specifically so a missed run (outage, transient error) can't create a permanent gap: an unresolved row just gets caught by the next day's run instead of falling through. The `guarantee_status = 'pending'` condition (checked again on the write, not just the read) is what actually makes this idempotent — a row only appears once, ever, whichever run happens to catch it. Deliberately has no `search_status` filter: a row later marked `'switched'` already solidified and collected its fee, and still gets its own Day-30 resolution based on whatever offers arrived on it before the switch.
    - **Anchor changed from `paid_at` to `solidified_at` (2026-08-12)** — the guarantee clock starts once the customer has locked in make/model and all refinement decisions, not at the moment of payment (see "The guarantee — exact rule" above). This is why the auto-solidify job above had to be built first: `solidified_at` previously had zero writers, so switching this query to it without that job would have silently made the Day-30 job process zero rows, forever, for every customer. `LEVR-Auto-Core-Processes-v1.md` §2 and `LEVR-Auto-Business-Plan-and-Roadmap.md` updated to match.
    - **Resolved (2026-08-13): upgraded to Vercel Pro, hourly cron confirmed live.** The caveat above was correct — Hobby tier does cap all cron jobs to once/day, and that's exactly what was silently happening. Layered on top of that: the hourly-cron code (this job, plus the Day-30 anchor fix) had only ever existed as uncommitted local changes, never actually pushed. Fixed by upgrading to Vercel Pro ($20/mo, via the dashboard billing UI) and pushing the previously-local-only code. Confirmed all 4 cron jobs registered correctly in the Vercel dashboard's Cron Jobs tab post-deploy, including this one. **Verified with real production data, not just a dashboard check:** created a real temporary test row already past its 24h window, confirmed via direct DB query it was still unsolidified beforehand, waited for the actual next hourly firing (not a manual trigger), and confirmed afterward the job had correctly solidified it (`solidified_at` set, `search_status` → `'searching'`). Test row cleaned up after.
    - `assessSearch` loads the row's `qualifying_offers`, runs the existing `evaluateOfferGuaranteeContribution` (unmodified) on each, and sets `guarantee_status = 'met'` if any evaluates to `'counts'`, else `'refunded'`. The `'refunded'` case only ever sets that status — it never calls Stripe. `guarantee_status = 'refunded'` is itself the worklist for a human to go process the actual refund. Originally verified (2026-08-11, against the since-changed `paid_at` anchor) with real (temporary) data covering six cases hit via the real HTTP route: a 35-days-paid row with zero offers → `refunded`; a 30-days-paid row with a below-MSRP delivered offer never sold → `met`; a 31-days-paid row whose only offer was above MSRP → `refunded`; a 10-days-paid row (not due yet) → correctly untouched/still `pending`; a 40-days-paid row already at `guarantee_status = 'met'` → correctly untouched (proves the pending guard); a 32-days-paid `'switched'`-status row with a qualifying offer → `met` (proves no `search_status` dependency). Also confirmed a second immediate run assessed zero rows (idempotency) and that the route 401s without the correct `CRON_SECRET`.
    - **Anchor-change re-verified (2026-08-12)** against real (temporary) data, hitting both real HTTP routes: a `pending_refinement` row paid 25h ago → solidify job correctly solidified it (`solidified_at` set, `search_status` → `'searching'`); a `pending_refinement` row paid 1h ago → correctly left untouched by the solidify job. **The critical case:** a row already `search_status = 'searching'` with `paid_at` 40 days ago but `solidified_at` still null (simulating data from before this job existed) → the Day-30 job correctly did **not** assess it, proving the anchor genuinely changed — under the old `paid_at`-based query this row would have wrongly resolved. Two rows solidified 31 days ago — one with a below-MSRP delivered never-sold offer, one with none — correctly assessed `met` and `refunded` respectively, with `guarantee_resolved_at` stamped on both. A second immediate run of both jobs assessed/solidified zero rows (idempotency), and both routes 401 without the correct `CRON_SECRET`. Test data (1 auth user, 5 `customer_searches` rows, 1 `qualifying_offers` row) cleaned up after.
  - Refunds Due — `/internal/refunds-due`, same `requireAgent()` gate as `/internal/outreach`. Read-only worklist (`getRefundsDueQueue`, `src/lib/refunds-due.ts`) of every `customer_searches` row at `guarantee_status = 'refunded'`, joined to the owning customer's name/email, showing make/model/trim and a "resolved" timestamp. No mark-as-processed action — the actual Stripe refund stays a deliberately separate, manual step, same as the Day-30 job above. The "resolved" timestamp is `guarantee_resolved_at` (`supabase/migrations/20260811090000_guarantee_resolved_at.sql`), a dedicated column set only by `assessSearch` (`src/lib/guarantee-assessment.ts`) at the exact moment `guarantee_status` transitions from `pending` — not `updated_at`, which every write path on the row touches and would drift the instant something unrelated (e.g. a post-resolution switch-make/model request) updated the row again. Verified end-to-end in a real browser as a real test agent against real (temporary) data: a `'refunded'` row rendered correctly with the right customer name/email/search/timestamp, while sibling `'pending'` and `'met'` rows for the same customer were correctly excluded; unauthenticated access correctly redirected to `/login`. Separately proved the drift-immunity directly: after the Day-30 job set `guarantee_resolved_at`, an unrelated write to the same row (a `notes` edit) bumped `updated_at` as expected but left `guarantee_resolved_at` byte-identical. Test data cleaned up after. Intentionally small — the fuller admin views (build-order item 12) are still a separate, later piece.
  - Add-on negotiation — the last piece of change-request logic. New `offer_addons` table (`supabase/migrations/20260811100000_offer_addons.sql`): itemized fees on a `qualifying_offers` row (`description`, `amount_cents`), added by an agent any time after the offer itself is logged (not forced into the initial `logQualifyingOffer` form — dealers often only reveal a fee breakdown once actually asked). This also closes a pre-existing documented gap: `LEVR-Auto-Core-Processes-v1.md` already said dealer offers must be itemized and the dashboard should show add-ons, but `qualifying_offers` only ever stored a total price.
    - Customer flow: `requestAddonRemoval` (`src/lib/addon-actions.ts`) flags a specific add-on for removal — routes to the agent via the outreach queue, never automated. Surfaced on `/account` (`AddonRemovalButton`) next to each add-on. Ownership re-checked server-side through `offer_addons → qualifying_offers → customer_searches.customer_id`, same pattern as `respondToOffer`.
    - Agent flow: `logOfferAddon` (itemize a fee) and `resolveAddonRemoval` (record what the dealer said: accepted/declined/countered, plus an optional note) in `outreach-actions.ts`, surfaced on `/internal/outreach` via `AddOfferAddonForm` and `ResolveAddonRemovalForm`.
    - **Deliberate scope decision, not an oversight:** `removal_status`/`dealer_response`/timestamps live directly on the `offer_addons` row — one active/most-recent negotiation round per add-on, not a full history thread. A customer can re-request after `dealer_declined`/`dealer_countered` (a new round), which overwrites the prior round's `dealer_response` and timestamps; only the latest round is ever visible. This doesn't block adding a proper rounds table later — that's additive (a new table + an extra insert alongside the existing update, no rework of `offer_addons`'s existing columns or the pages/queries that read them) — but it does mean any round that already happened and was already overwritten before a rounds table existed can never be reconstructed. Revisit if real usage shows agents actually need prior-round history.
    - Verified end-to-end in a real browser, sequentially as a real test agent and a real test customer (two simultaneous sessions aren't possible — Supabase Auth cookies are shared per-browser-profile across tabs, so concurrent logins in separate tabs silently clobber each other's session): agent logged a real add-on, customer saw it and requested removal (confirmed `removal_status = 'pending'` in the DB), agent resolved it as declined with a note (confirmed `dealer_response` and `removal_resolved_at` set), customer saw the decline reason and the "Request removal" button reappear, customer re-requested (round 2) — confirmed in the DB that `removal_status` reset to `'pending'`, `dealer_response` reset to `null`, and `removal_requested_at` advanced to a new, later timestamp. Test data cleaned up after.
  - Communication preferences, captured at signup — two new `customers` columns (`supabase/migrations/20260811110000_communication_preferences.sql`): `communication_frequency` (`'real_time'` / `'daily_digest'`, default `'real_time'`) and `communication_channel` (`'text'` / `'email'` / `'agent_callback'`, default `'email'`). Deliberately **capture-only**: no notification-sending system reads these yet — texts/emails actually going out on a real-time or digest cadence is a separate, later build. Threaded through the existing `handle_new_user()` DB trigger the same way `full_name`/`phone` already were, with `coalesce(..., 'real_time')` / `coalesce(..., 'email')` fallbacks in the trigger so any signup path that omits a field still gets a sane default. Both `text` and `agent_callback` need a real phone number to be usable (a personal agent can't call back with no number, same as a text can't send with no number) — `channelRequiresPhone()` (`src/lib/communication-preferences.ts`, a plain module with no `"use server"`, since `auth-actions.ts` requires every export to be an async Server Action and can't hold a sync helper) is the single shared source of truth for that rule, enforced server-side in both `signup()` and `signupInline()` (`src/lib/auth-actions.ts`).
    - **Both signup entry points now collect channel** (not just `/signup`) — the compact `AuthGateModal` used mid-intake-flow gained a channel `<select>` (default Email, zero added friction for anyone who doesn't touch it) and a phone field that only appears when `text`/`agent_callback` is chosen, mirroring `/signup`. **Frequency stays out of the modal on purpose** — per the design discussion, it doesn't gate any other field the way channel gates phone, and can reasonably wait for a future account-settings page; it simply isn't sent from `signupInline`, so it defaults to `'real_time'` via the same trigger fallback used everywhere else.
    - **Bug fix found and closed in the same pass:** `/signup`'s original phone-required check only covered `channel === 'text'`, not `agent_callback` — a real gap, since a callback is equally uncollectable without a number. Both `/signup` and the modal now use the same `channelRequiresPhone()` check.
    - Verified end-to-end with real signups through both real UI paths (not shortcuts) — the modal was driven through the actual homepage intake flow (make/model/zip → Continue → auth gate), not a direct navigation: `text` with no phone was correctly blocked (native HTML `required` validation, confirmed via DB check that no account was created — this made the client-side JS error banner redundant in this exact path since native validation intercepts first, but the outcome is what matters); `agent_callback` with no phone was also correctly blocked (proving the bug-fix path in the modal too); `text`+phone and `agent_callback`+phone both produced correct `customers` rows (right channel, right phone, frequency defaulted to `'real_time'`); the untouched-default path (`email`, no phone field ever shown) produced a correct row with `phone = null`. Separately confirmed `/signup`'s fixed guard: submitting `agent_callback` with no phone (that field has no HTML `required` there, so this hits the real server-side check, not native validation) correctly redirected with a visible error and created no account. Test data cleaned up after.
  - Financing/document flow, non-blocked pieces only — availability re-confirmation, deposit recording, and financing capture (Build order item 10). The e-sign piece (PandaDoc) is a separate, later addition once a sandbox API key exists — see "Not built yet" below.
    - **Deposit is record-keeping only, never a LEVR-processed payment.** The reservation deposit goes directly from customer to dealer; LEVR never touches this money. `deal_progress.deposit_amount_cents` is whatever the agent is told the dealer collected (varies per deal, not a LEVR-set fee), and `deposit_confirmed_at` is set manually by the agent once the dealer confirms receipt — same pattern as `qualifying_offers.vehicle_sold_at` (a nullable timestamp *is* the boolean; no separate confirmed flag alongside it). No Stripe Checkout Session, no webhook, no refund-execution logic for this piece.
    - New `deal_progress` table (`supabase/migrations/20260811120000_deal_progress.sql`), 1:1 via unique FK to `qualifying_offers`, created lazily by whichever agent/customer action fires first (upsert on `qualifying_offer_id`, so `confirmAvailability`/`confirmDepositReceived`/`submitFinancingChoice` can happen in any order without clobbering each other's columns): `availability_reconfirmed_at`, `deposit_amount_cents`, `deposit_confirmed_at`, `financing_choice` (`'own'`/`'help'`), `financing_income_range`, `financing_down_payment_cents`, `financing_desired_term_months`.
    - New `documents` table (1:many per offer — a deal can have multiple), `type` currently only `'financing_proof'` (`'service_agreement'` is the later PandaDoc addition, same table, no schema change needed for it). Backed by a new private Supabase Storage bucket (`documents`) — first real use of Storage in this project. No `storage.objects` RLS policies at all; uploads/reads go only through the `service_role` admin client server-side, same "no client-facing write path" convention as every other table here.
    - Financing is pure data capture, matching Core-Processes-v1.md exactly — never a credit pull (FCRA-regulated, needs a compliant vendor partnership later). `submitFinancingChoice` (`src/lib/deal-progress-actions.ts`) branches on the customer's choice: `'help'` just writes the three preference fields; `'own'` uploads the file directly via the Server Action (never a client-side Storage upload) to the `documents` bucket and inserts a `documents` row. Freely re-submittable — this isn't a state machine, a later submission just overwrites/adds via upsert, matching the change-of-mind case (a customer who resubmits from "help" to "own" correctly flips `financing_choice`).
    - Agent UI (`/internal/outreach`, only on offers at `status = 'customer_accepted'`): `ConfirmAvailabilityButton` (mirrors `MarkSoldButton` exactly) and `ConfirmDepositForm` (amount input + one button — sets both deposit fields together, since realistically both come from the same phone call with the dealer, not two separate steps). Read-only visibility into the customer's financing choice, including a short-lived (15m — tightened from an initial 1h after a deliberate check-in on how sensitive these files are) signed Storage URL to view their uploaded proof, minted fresh on every page load and never persisted anywhere.
    - Customer UI (`/account`, same gate): a "Congratulations — next steps" panel showing read-only availability/deposit status (agent-set) plus `FinancingCaptureForm` — a toggle between the two financing paths, conditionally rendering either the file input or the three preference fields.
    - Verified end-to-end in a real browser, sequentially as agent then customer (same cross-tab-session constraint as before): agent confirmed availability and entered/confirmed a $500 deposit — both read back correctly as read-only status text; customer submitted the "help" financing path (income range, down payment, desired term) — confirmed all three fields correct in the DB; customer then switched to "own" and uploaded a real file — confirmed in the DB that `financing_choice` flipped from `'help'` to `'own'`, a real `documents` row was created, and the actual file bytes in Storage matched byte-for-byte via a direct download-and-compare (not just checking upload succeeded); agent's view then showed the updated financing state and a working signed URL — fetched directly with `curl`, no auth, confirming it actually serves the real uploaded content, not just that a URL string was generated. Test data (2 auth users, 1 `customer_searches` row, 1 accepted `qualifying_offers` row, 1 `deal_progress` row, 1 `documents` row + its Storage object, 1 `agents` row) cleaned up after.
- **Not built yet:** the monthly low-movement sync tier (see above), outreach automation (deliberately manual for now), dealer-reply parsing, actually processing the Stripe refund for `guarantee_status = 'refunded'` rows (deliberately manual for now — see Day-30 assessment job above), the notification-sending system that actually acts on `customers.communication_frequency`/`communication_channel` (captured at signup now, nothing reads it yet), the $100 switch fee / 5-day grace period, the LEVR e-sign document (PandaDoc — blocked on a sandbox API key; the `documents` table/Storage bucket already support a `'service_agreement'` type, just unused so far), delivery coordination, fuller admin views — see Build order below for the sequence
  - **Re-verified against production, not localhost.** Supabase's Auth "Site URL" had been misconfigured to `localhost:3000` — meaning earlier local-only verification of signup confirmation/password reset could have been passing partly by accident (a redirect not on the allow-list can silently fall back to Site URL, which happened to equal what local testing wanted anyway). Fixed to `https://levrauto.com`. While in there: also found `NEXT_PUBLIC_SITE_URL` in Vercel Production was unreadable (masked by the CLI, same class of risk as the earlier MarketCheck key incident), so re-set it explicitly to `https://levrauto.com` and redeployed rather than trust the existing value. Re-ran both flows for real with the local dev server stopped (`lsof -i :3000` confirmed no listener) so there was no possible localhost fallback: a genuine signup via `https://levrauto.com/signup` → confirmation link → landed signed-in on `https://www.levrauto.com/account`; a genuine password reset via `https://levrauto.com/forgot-password` → reset link → new password set → logged in successfully on production. Both clean. Test account deleted after. (Caveat: link clicks were simulated via `admin.generateLink` with the production redirect explicitly matching what the app should send, same method used throughout this session, since there's no way to read a real inbox directly — this proves the `/auth/callback` and `/auth/reset-password` mechanisms work correctly on the live domain, but doesn't by itself prove what `NEXT_PUBLIC_SITE_URL` was before the explicit re-set — which is exactly why it was re-set rather than left to trust.)

## Pricing Pivot Tracking (updated 2026-08-15)

Tracking the remaining pieces of the pricing pivot (flat $699, one vehicle) as a sequence — not the same numbering as the finalization-flow "pending pivot Steps 1-6" referenced in code comments (`intake-actions.ts`, `intake-filter.tsx`, `search-solidification.ts`), which is a separate, already-shipped piece of work.

- **Step 1 — Guarantee clock retiming: confirmed already correct as of the Aug 13 commits, no fix needed.** A 2026-08-14 discovery pass (prompted by a suspicion the Day-30 job might still key off `paid_at`) found `getDueSearches` in `src/lib/guarantee-assessment.ts` already keys off `solidified_at`, and `solidified_at` itself is already anchored to `finalized_at + 24h` (not `paid_at`) via `search-solidification.ts`, since the Aug 13 finalization-flow work. A row still at `search_status = 'awaiting_finalization'` (finalized_at null) can never acquire `solidified_at`, so it's structurally excluded from Day-30 assessment — confirmed by reading the query chain directly, not inferred. No code changes were made or needed.
- **Step 2 — Pre-payment inventory display: complete (2026-08-14), fully verified against real production data.** Decision locked in: **real zip-radius filtering is required, not a nationwide-only count** — business reason (per Brett): local sourcing keeps transport cost/coordination lowest. Zero-match combos show an honest zero with reassurance copy that outreach can still source it — no gating on submit either way (`canSubmit` doesn't gate on match count). The fabricated `match-counter.ts` (hardcoded per-make table + seeded hash, never queried real data) is fully deleted and replaced by `src/lib/inventory-count.ts` (`countNearbyInventory`, a Server Action: `listings` filtered by make+model → distinct `dealer_zip`s + customer zip looked up in `zip_coordinates` in one query → Haversine distance in application code → count within `INVENTORY_RADIUS_MILES` (`src/lib/inventory-radius.ts`, split into its own file since a `"use server"` file may only export async functions), wired into `IntakeFilter`/`MatchCounter` in `intake-filter.tsx` with a 400ms-debounced fetch replacing the old synchronous fake calculation. Listings with an unresolvable `dealer_zip` are excluded from the count, not errored. **Real end-to-end test results:** zip `90210` against the 139 total synced Honda Civic listings returned **8 genuine within-radius matches, 7.1–41.7 miles** (all geographically sane); zip `99926` (Ketchikan, AK) against the same 139 listings returned a **genuine 0 — every listing correctly excluded at 1,200+ miles**, not a fallback number. See discovery/build findings below for the full trail.
- **Step 3a — Live pricing correctness: confirmed clean (2026-08-14).** Flat $699 is consistent everywhere checked: intake/landing page (`FLAT_PRICE = 699` in `src/lib/vehicle-data.ts`, no tiered UI remnants), `faq/page.tsx` (three answers), `guarantee.tsx` copy, and DB schema (`package_size` fully dropped, no tier/plan field of any kind on `customer_searches`). No dedicated `/pricing` route exists; `account`/`finalize`/`site-footer` have no hardcoded pricing text at all, so nothing there to go stale. Stripe Checkout (`payment-actions.ts:createCheckoutSession`) builds the line item inline via `price_data` every time (`unit_amount: FLAT_PRICE * 100`) — never references a stored Stripe Dashboard Price ID, so even if orphaned $899/$999 Product/Price objects exist in the Stripe test-mode Dashboard from earlier tiered-pricing testing, they're structurally unreachable from checkout, not "live." Dashboard itself wasn't directly inspected — no Stripe API credentials available in this session (same redaction as Supabase), would need Brett to check the Dashboard directly if that residue matters.
- **Step 3b — $100 switch fee + 5-day grace period: backend plumbing AND self-service UI + call-request queue both built and verified against real data (2026-08-14).** Confirmation copy for the three customer-facing screens is locked (see "Step 3b confirmation copy" below). Three switch entry points, all live: (1) self-service from `/account` — pick new make/model → check grace-period eligibility (`customers.created_at + 5 days` and `free_switch_used_at is null`) server-side → if free, show the locked free-switch confirmation and execute immediately; if not free, show the locked $100-fee warning (real Checkout Session creation for this case is a separate follow-up — the button is a disabled placeholder for now); (2) customer requests a call instead, surfacing in `/internal/outreach`'s new "Switch calls requested" section alongside the finalization-call queue; (3) the existing agent-initiated path (`/internal/outreach`, `switch-actions.ts`), kept as a free/comped alternative an agent can always use.
  - **Schema — columns on `customer_searches`/`customers`, no new table, no new `search_status` value** (same reasoning as finalization's `call_requested_at`): `pending_switch_make`/`pending_switch_model`/`switch_requested_at`/`switch_call_requested_at` (all nullable) on `customer_searches`, `free_switch_used_at` (nullable) on `customers`. Migration proposed and applied: `supabase/migrations/20260814130000_switch_fee_flow.sql`.
  - **`pending_switch_make`/`model` are display-only, deliberately not the webhook's source of truth** — a customer could overwrite them with a second switch request before an earlier, still-in-flight Checkout Session's webhook event lands (Stripe retries/late delivery). The webhook reads `new_make`/`new_model` straight from the Checkout Session's own metadata instead, set once at session-creation time and immutable after.
  - **Real gap fixed, not just designed around:** `switch_customer_search` previously always left the new row's `paid_at` at its column default (null), so a switch — free or paid — would still hit `/finalize`'s `if (!paid_at) redirect("/")` guard and wrongly tell the customer to pay $699 again, defeating the entire point of the $100-fee-replaces-a-second-payment policy. Fixed by adding `p_paid_at timestamptz default null` to the RPC, included in the new row's insert. **Decision locked in:** the agent-initiated path also now passes `p_paid_at := now()` (previously it didn't set this at all) — agent-comped switches stay free, but now reach `/finalize` correctly instead of hitting the same wall.
  - **Decision locked in: agent-comped switches now also set `customers.free_switch_used_at`** (closes the "double-free" gap — previously an agent comping a switch didn't count against the one-free-switch policy at all, meaning a customer could get a free agent switch *and* still qualify for a free self-service switch later). Written as a guarded "set only if null" update (`.is("free_switch_used_at", null)`) — the practical equivalent of `coalesce(free_switch_used_at, now())`, since supabase-js's `.update()` can't reference an existing column's value, only set static ones. First switch's timestamp always wins.
  - **Webhook extended, not split into a second endpoint** (`src/app/api/stripe/webhook/route.ts`) — a `metadata.type` discriminator (`"search_payment"` | `"switch_fee"`) added to both the existing $699 Checkout Session (`payment-actions.ts`) and read at the top of the existing `checkout.session.completed` handler; missing `metadata.type` defaults to `"search_payment"` for backward compatibility with any Checkout Session created before this field existed. Reasoning for one endpoint, not two: there's exactly one registered webhook endpoint in the Stripe test-mode Dashboard (`we_1U2cbg8FQdYEFttXePzCHvX8`) and no isolation benefit to a second one, just two things to keep in sync.
  - **Idempotency for Stripe webhook retries** — `switch_customer_search`'s existing not-already-switched guard raises a plain `raise exception 'search % has already been switched', ...` with no explicit SQLSTATE, which PL/pgSQL defaults to `P0001` (confirmed by reading the RPC source, not assumed — the RPC's *other* exception, "row % not found," also raises `P0001`, which is why the webhook's matcher checks the message text too, not just the code, so a genuine not-found error can't be silently swallowed as "already handled"). `isAlreadySwitchedError()` in the webhook route catches this specific case, logs it, and returns `200` rather than erroring on a Stripe retry landing after the switch already succeeded.
  - **Deliberately deferred, logged not built:** the `search_payment` webhook branch kicks off an on-demand `syncListingsForMakeModel` call right after setting `paid_at`, so `/finalize` has real trim data by the time the customer lands there. The new `switch_fee` branch does not do this yet — a freshly-switched search will hit `/finalize`'s existing empty-listings fallback instead of real synced data for the new make/model, until this is built.
  - **Backend plumbing verified against real data (2026-08-14).** Migration `20260814130000_switch_fee_flow.sql` run successfully in production. The temporary diagnostic route (`src/app/api/internal/scratch-switch-fee-test/route.ts`) was run against a real local dev server with real credentials: `switch1_paidAtWasSet`, `freeSwitchUsedAt_unchangedAcrossSwitches`, and `retrySwitch_matchedByIsAlreadySwitchedError` all confirmed `true` — proving the new row's `paid_at` gets set correctly, the free-switch timestamp is first-write-wins across chained switches, and the webhook's `isAlreadySwitchedError()` matcher correctly identifies the real Postgres exception `switch_customer_search` raises on a retry. Scratch route deleted after use (confirmed via grep — no remaining references anywhere in `src/` or `supabase/`); `api/internal/` now only contains the legitimate `sync-listings` route.
  - **Still not tested: the full Stripe-webhook round trip** (a real `switch_fee` Checkout Session → `stripe listen` → real delivery, plus a replay to prove the retry path) — deferred until the self-service Checkout-creation code that would produce a real `switch_fee` session exists (separate follow-up pass, along with the self-service UI and call-request queue noted above).
  - **Self-service switch UI + call-request queue: built and verified (2026-08-14).** Mirrors `FinalizeChoice`'s exact shape (one client component, local `useState` modes, seeded from an already-requested prop) per the UI discovery pass. New: `src/components/switch-choice.tsx` (`SwitchChoice`, modes `"choice" | "pick" | "free-confirm" | "paid-warning" | "call-requested"`), `src/lib/switch-self-service-actions.ts` (`requestSwitchCall`, `checkSwitchEligibility`, `executeFreeSwitch` — all customer-owned, mirroring `finalize-actions.ts`'s shape), a new `getSwitchCallQueue` in `outreach-queue.ts` + a "Switch calls requested" section in `/internal/outreach` mirroring "Finalization calls requested" exactly (same `AgentSwitchSearchForm` resolves it — no new agent action needed), and `SwitchChoice` wired into `/account`'s `SearchCard` for any paid, non-`switched`/`closed` search. Grace-period eligibility (`created_at + 5 days` and `free_switch_used_at is null`) is computed server-side in `checkSwitchEligibility`, re-checked again inside `executeFreeSwitch` itself (never trusts the earlier check — a customer could sit on the confirmation screen long enough to cross the window). The $100-warning screen's "Continue to payment" button is a disabled placeholder — real Checkout Session creation is the next pass, per instruction.
  - **Redirect target confirmed by reading the migration, not memory:** the new row's `search_status` lands at its column default, which is `'awaiting_finalization'` (set by `20260813180000_finalization_flow.sql`, not touched by any later migration including this one) — combined with `p_paid_at` now setting `paid_at`, `/finalize/[newSearchId]`'s two guards (`paid_at` set, `search_status === 'awaiting_finalization'`) both pass, so `executeFreeSwitch`'s redirect target is correct.
  - **Bug fixed in the same pass, same data path (`getCustomerDashboard`/`/account`):** an abandoned original-checkout row (`awaiting_finalization`, `paid_at: null` — a real, reachable state since intake creates the row before the Stripe redirect, and `getCustomerDashboard` never filtered on `paid_at`) previously rendered "Payment received — finalize trim, color, and options to start your search" with a live "Finalize this search" link that dead-ended at `/finalize`'s own unpaid guard. `getStatusCopy()` in `account/page.tsx` now branches on `paidAt` first: an unpaid `awaiting_finalization` row shows "Checkout wasn't completed — this search hasn't been paid for, so it hasn't started." with a "Head back to the homepage to try again" link instead of the dead-ended Finalize link. Copy proposed and applied in the same pass, not separately signed off first — flagging clearly here in case Brett wants it adjusted. `canSwitch()` also gates on `paidAt !== null`, so this same unpaid state correctly shows no switch UI either.
  - **Verified against real browser click-throughs plus DB state (2026-08-14).** All four scenarios confirmed correct: free-switch path (customer A) executed end to end through the real UI and redirected to `/finalize/[newId]` correctly; $100 warning screen (customer B) rendered instead of the free-case copy for a customer backdated past the 5-day grace window; call-request path set `switch_call_requested_at` and — critically — `switchCallQueue_matchingRows` confirmed the request actually surfaces in `/internal/outreach`'s new "Switch calls requested" section, not just that the column got written; the `/account` bug fix (below) rendered the correct copy for the unpaid row with no dead link and no switch UI.
  - **Link-generation bug found and fixed during this verification pass.** The scratch route's `setup` mode originally generated two magic-link OTP tokens for the *same* email (`emailB`) back to back in one `Promise.all`, for two separate manual test cases. Supabase/GoTrue stores a magic-link token as a single column value per user, not a table of concurrently valid tokens — generating the second link silently invalidated the first the instant it was created, regardless of whether the first had been clicked yet. Customer B's first link failed "invalid or expired" on its very first click as a direct result — confirmed by re-reading the diff that introduced it, not by guessing (ruled out a Strict-Mode double-`verifyOtp` explanation by confirming `/auth/confirm` calls `verifyOtp` only from an explicit button `onClick`, no `useEffect`, anywhere in the component). Fixed by minting each email's link lazily, one `generateLink` call at a time, right where each is used — never two for the same email in the same batch.
  - **Also caught while building the fix: `admin.generateLink()`'s `action_link` field is the wrong thing to hand to a browser here at all**, separately from the double-token bug — it points at Supabase's own hosted `/auth/v1/verify` endpoint (the GET-consumes-token endpoint `/auth/confirm` exists specifically to avoid), and its `redirect_to` lands tokens in a URL hash fragment that `/account` has no code to parse. Links are now built from `properties.hashed_token` directly into `/auth/confirm?token_hash=...&type=magiclink&next=...`, the same mechanism the real signup/recovery email links use.
  - **`/account` unpaid-row bug fix, copy proposed and applied:** `getStatusCopy()` now shows *"Checkout wasn't completed — this search hasn't been paid for, so it hasn't started."* (with a "Head back to the homepage to try again" link) for an `awaiting_finalization` row with `paid_at: null`, replacing the previously-wrong "Payment received..." text and dead-ended "Finalize this search" link. **Badge polished in the same pass** (found during this verification round: the status badge still showed "AWAITING FINALIZATION" for this same unpaid case, visually contradicting the corrected body copy) — `getStatusBadge()` shows `"checkout incomplete"` (renders as `CHECKOUT INCOMPLETE` via the existing `uppercase` CSS class, matching the existing badge's casing convention) for the same unpaid condition.
  - **Scratch route deleted after use** (`src/app/api/internal/scratch-switch-ui-test/route.ts`) — confirmed via grep no remaining references in `src/`/`supabase/`; `api/internal/` again contains only the legitimate `sync-listings` route. Test data cleanup (3 temp auth users/customers/searches, emails matching `scratch-switch-%@example.invalid`) handed off as SQL for Brett to run directly in the Supabase SQL Editor, same as every other DB-write this session couldn't execute itself — the scratch route (this session's only mechanism for reaching real data) was deleted per instruction before cleanup could run through it, and the real customer/search ids from the `setup` call were only ever returned directly to Brett, never pasted back into this conversation.
- **Step 4 — Day-60 extension logic ($100 per ~30-day extension after Day 60).** Originally new/unbuilt — grepping the whole `src/` tree found only a hardcoded label in `src/components/guarantee.tsx` (marketing copy), no cron/query/anchor at all. **Policy finalized, and schema + email infrastructure + crons + queue + extension payment flow all built and verified (2026-08-15) — dashboard UI is the one remaining piece.**
  - **Policy:** anchored to `solidified_at` (same anchor as Day 30), but the deadline shifts forward 30 days with each paid extension — not a one-time boundary. 7 days before the current deadline: dashboard note (computed at read time, no stored state) + email reminder ("extend anytime for 30 more days"). At the deadline, hard cutoff, no grace: `search_status` → `'paused'` — the first real writer of this previously-unreachable value (see the `search_status` exhaustive-trace bullet above). 7-day self-service window after pausing: customer can pay $100 to resume immediately, deadline extends 30 days from whatever it was. After that window closes: no automatic status change — surfaces for an agent to handle manually (comp, write off, etc. — a decision for the payment-flow build pass, not this one); if nothing happens, the customer's practical path is starting a fresh $699 search. Auto-renew (opt-in future automatic charges) is explicitly deferred to its own follow-up.
  - **Deliberate non-decision: no `qualifying_offers.status` soft-exclusion anywhere the reminder/pause logic reads searches.** That field could have the same "legal per the schema but never actually written to the value you'd check for" problem `search_status`'s `paused`/`closed` had before this pass — without the same exhaustive write-site audit for it, checking it would be false confidence, not real protection. Related: **no real "purchased"/deal-closed signal exists anywhere in the schema** (confirmed by grep, not assumed) — logged as a concrete spec under the roadmap doc's Customer Journey step 8 for when the deposit → close pipeline actually gets built, not built now. Until it exists, the Day-60 job cannot distinguish an already-closed deal from a search that's simply gone quiet — a known, named limitation, not an oversight.
  - **Migration proposed for review, not run:** `supabase/migrations/20260814160000_day60_extension_flow.sql` adds `search_deadline_at` (nullable — null means "compute default from `solidified_at + 60 days`"; set explicitly on first extension, +30 days each subsequent one), `paused_at` (set by the pause cron, anchors the 7-day resume window), `deadline_reminder_sent_for` (dedup by *which deadline value* the reminder was sent for, not a boolean — self-correcting when the deadline moves, since a new deadline naturally stops matching the stored one), and `last_extension_session_id` (idempotency guard for the extension webhook branch — `search_deadline_at` changes repeatedly over a search's life, unlike `paid_at`, so a one-shot `IS NULL` guard doesn't work; same "condition that becomes false after the first success" idiom used for `paid_at`/`call_requested_at`/`free_switch_used_at`, see the payment-flow build bullet below for why the actual guard isn't a plain `.neq()`).
  - **`src/lib/email.ts` — new, general-purpose ZeptoMail HTTP API wrapper, not Day-60-specific.** `sendEmail({ to, subject, html, toName })`. Confirmed by reading `.env.local`'s actual key names (not guessed) that **no ZeptoMail API key env var exists in this project today** — every email sent so far (signup confirmation, password reset) goes through Supabase Auth's own built-in templates, configured with ZeptoMail as the SMTP backend at the Supabase dashboard level, never through application code. This is genuinely new capability. Needs `ZEPTOMAIL_API_KEY` (the full "Zoho-enczapikey ..." string ZeptoMail's console gives you to copy, pasted verbatim as the `Authorization` header — not just the bare token) and `ZEPTOMAIL_FROM_EMAIL` added to `.env.local` and Vercel before this can send real mail; `ZEPTOMAIL_FROM_NAME` is optional (defaults to "LEVR Auto"). **Not set anywhere yet — blocks real end-to-end testing of the reminder cron until added.**
  - **`src/lib/day60-extension.ts`** — `sendDay60Reminders()` and `pauseOverdueSearches()`. Both fetch the small set of `search_status = 'searching'` rows with `solidified_at` set and compute/filter the effective deadline in application code (`effectiveDeadline()`), rather than filtering in the DB query itself — PostgREST (what supabase-js talks to) can't filter on a `coalesce`/interval expression, so this follows the same convention already used by `getOverdueFollowUpQueue`'s hours-overdue math and `inventory-count.ts`'s Haversine calculation: fetch a small, index-backed set, compute in JS.
  - **Two new cron routes**, same `Authorization: Bearer $CRON_SECRET` pattern as every other cron route, both registered in `vercel.json`: `/api/cron/day60-deadline-reminder` (daily, `0 8 * * *`, alongside Day-30) and `/api/cron/day60-pause-overdue-searches` (daily, `0 9 * * *`).
  - **`getStalePausedSearchesQueue`** (`outreach-queue.ts`) — `search_status = 'paused'` and `paused_at <= now() - 7 days`. New "Paused past resume window" section on `/internal/outreach`, same visual pattern as the other queues (make/model, customer email, a days-paused label), no action form yet.
  - **Verified end-to-end against real data (2026-08-14).** Via the scratch test route (`src/app/api/internal/scratch-day60-test/route.ts`, deleted after use): reminder send confirmed by real inbox delivery (not just a 200 from the API); dedup confirmed by running the reminder cron a second time immediately and getting zero sends, since `deadline_reminder_sent_for` already matched the computed deadline; the pause cron confirmed transitioning a real overdue test row (`search_status` → `paused`, `paused_at` set) while leaving the not-yet-due row untouched; `getStalePausedSearchesQueue` confirmed correct — included the row paused 8 days ago, excluded the row paused moments earlier by the pause cron itself (0 days paused, not yet past the 7-day resume window), with `fullStalePausedQueueLength` confirming no false positives beyond the one expected row. **ZeptoMail HTTP API integration confirmed working end-to-end with the real Send Mail Token** — `ZEPTOMAIL_API_KEY` needed the full `Zoho-enczapikey <token>` string as the literal `Authorization` header value (confirmed by reading `src/lib/email.ts`'s exact header-construction code before Brett pasted the real token, not guessed) — `email.ts` uses `process.env.ZEPTOMAIL_API_KEY` directly as the header with no prefix added in code, so the env var itself must carry the `Zoho-enczapikey ` prefix.
  - **Reminder email copy locked and applied.** Subject: `Your search pauses on [date] unless extended`. Body: *"Your LEVR Auto search for a [make] [model] will stop on [date] — we will be unable to continue searching for new offers unless you extend. Extending is easy: just [log into your account] anytime before then, and we'll keep actively searching for 30 more days."* `[log into your account]` is a real link to `/account`, built from `NEXT_PUBLIC_SITE_URL` with the same `?? "http://localhost:3000"` fallback already used in `auth-actions.ts`/`payment-actions.ts` — not a new convention. `[date]`/`[make]`/`[model]` were already the working template values (`deadline`, `search.make`, `search.model`) computed earlier in `sendDay60Reminders` — no new placeholders needed building.
  - **Extension payment flow built and verified (2026-08-15).** `EXTENSION_FEE = 100` added alongside `FLAT_PRICE` in `vehicle-data.ts`. `createExtensionCheckoutSession(searchId)` (`src/lib/extension-actions.ts`) mirrors `createCheckoutSession` exactly — inline `price_data`, no stored Stripe Price ID, `success_url`/`cancel_url` both back to `/account` (whether the page reflects the extension before the webhook finishes is a dashboard-UI concern, not this pass). Server-side-only eligibility, never trusting a client gate (same standard as `executeFreeSwitch`): `search_status = 'searching'` is always eligible; `'paused'` is eligible only if `paused_at` is within the 7-day resume window; anything else (a stale pause, wrong owner) is rejected with a clear error. The webhook's `metadata.type` discriminator gained an `"extension_fee"` case (`handleExtensionFeePayment`, `src/app/api/stripe/webhook/route.ts`), computing `newDeadline = effectiveDeadline(row) + 30 days` (reusing `effectiveDeadline`/`addDays`, now exported from `day60-extension.ts` rather than duplicated) and writing `search_deadline_at`, `search_status = 'searching'`, `paused_at = null`, `last_extension_session_id = session.id` together.
    - **Real deviation from the originally-approved design, flagged during the build, not silently changed:** the migration's own column comment (and the original build instruction) specified guarding the webhook's update with a plain `.neq("last_extension_session_id", session.id)`. Built differently on purpose — `last_extension_session_id` starts `NULL` on every search's *first* extension, and SQL's `NULL <> x` evaluates to `NULL` (falsy in a `WHERE` clause), so a bare `.neq()` would silently match zero rows and block every first-ever extension, not just retries. The actual guard is `.or("last_extension_session_id.is.null,last_extension_session_id.neq.<id>")` — same retry-proof idempotency, but doesn't break the first call.
    - **Verified end-to-end against real data (2026-08-15).** Via the scratch test route (`src/app/api/internal/scratch-extension-test/route.ts`, deleted after use): all four eligibility cases correct — a `'searching'` row and a `'paused'` row 3 days in both eligible, a `'paused'` row 10 days in (past the resume window) correctly rejected, and a row queried under a different customer's id correctly rejected as unverifiable. A real `checkout.session.completed` event (signed with the actual `STRIPE_WEBHOOK_SECRET` via Stripe's `generateTestHeaderString` and POSTed to the real `/api/stripe/webhook` route, not a copy of the handler) correctly computed `+30 days` off the existing deadline. The identical event replayed with the same session id correctly no-op'd (`skipped: "already processed"`, `search_deadline_at` unchanged) — proving the NULL-safe guard closes the idempotency gap without reintroducing the original bug. Extending the paused-but-within-window row correctly flipped `search_status` back to `'searching'` and cleared `paused_at`. Test data (2 auth users/customers, 3 `customer_searches` rows) cleaned up via cleanup mode, confirmed zero rows remained on a second run. Scratch route deleted after use, confirmed via grep no remaining references anywhere in `src/` — `api/internal/` again contains only the legitimate `sync-listings` route.
  - **Day-60 extension policy is now complete except for the dashboard UI** (surfacing the reminder window / paused state / an "extend now" action on `/account` — a separate, later pass).
  - **Dashboard banner copy locked (2026-08-15), ready to build next session — no code written this pass.** Matches the already-approved reminder-email copy under the new cross-channel-consistency principle (see "Approach & patterns" above): same underlying facts (deadline approaching / already paused, 30 more days on extend, self-service window), wording adapted per format — the email uses a fixed date, these banners use a live day-count.
    - **Reminder banner** (shown within the 7-day-before window, i.e. whenever the email reminder would also be eligible to send): *"Your search pauses in [X] days unless extended. Extend now to keep us actively searching for 30 more days."* Still current — not affected by the paused-state policy revision below.
    - ~~**Paused-state banner** (shown while `search_status = 'paused'` and within the 7-day self-service resume window): *"Your search is paused — you can resume it anytime in the next [X] days by extending. After that, you'll need to reach out to your agent."*~~ **Superseded by the finalized paused-state policy spec below (2026-08-15)** — that version assumed a 7-day resume window and named the agent as the after-expiry path; the finalized policy uses a 30-day window and deliberately removes any customer-facing hint of the agent bypass.
    - `[X]` is a live day-count, computed the same way `effectiveDeadline()`/the resume-window math already work in `day60-extension.ts`/`extension-actions.ts` — not a new calculation to design.

  - **Day-60 paused-state policy — finalized full spec (2026-08-15), to be built across sequenced passes, not started yet.**
    1. **Self-service resume window: 7 days → 30 days after pausing.** Changes `RESUME_WINDOW_DAYS` in `extension-actions.ts` (currently `7`, used by both `createExtensionCheckoutSession`'s eligibility gate and the paused-state banner math) and the equivalent window check anywhere else it's duplicated.
    2. **Paused-state dashboard copy (within window, live countdown):** *"Your search is paused. You have [X] days left to resume it by extending — after that, you'll need to start a new search."*
    3. **Expired-window copy (deliberately no hint of the hidden bypass):** *"This search has ended. To continue, you'll need to start a new search."*
    4. **Agent queue broadens from "stale paused only" to showing EVERY currently paused search**, with live days-remaining per row — agents stay aware throughout the window, not just alerted once it's already stale. Supersedes `getStalePausedSearchesQueue`'s current 7-day-only filter (`outreach-queue.ts`) once built.
    5. **New automated reminder email**, mirroring `sendDay60Reminders`'s exact pattern (same `email.ts` utility, same dedup-by-deadline-value logic via a new `*_sent_for`-style column) but anchored to the pause window instead of the search deadline — fires once, 7 days before the (now 30-day) resume window closes.
    6. **New hidden agent bypass** — revives a paused search (waiving either the switch fee or the extension/continuation fee) without a real Stripe charge, invisible to any customer-facing text or FAQ anywhere, case-by-case only, agent discretion. Logged in its own dedicated audit table: agent, search, which fee type was bypassed, a reason, timestamp. Never referenced in customer-facing copy — this is the reason item 3's expired-window copy is deliberately silent on any path forward beyond "start a new search."
    - **Build sequence, this session:**
      - **Pass 1 (this session):** item 1 (window change, 7→30) + items 2–3 (copy) + re-verifying the payment-eligibility gate at the new threshold. **Not yet started as of this log entry** — a real, in-place policy change to already-shipped code (`extension-actions.ts`'s eligibility gate, verified just this session at the old 7-day threshold), so it needs its own explicit build-and-reverify pass, not silently folded into this doc update.
      - **Pass 2:** items 4–5 — broadened agent queue (every paused search, not just stale ones) + the new pause-window reminder cron.
      - **Pass 3:** item 6 — the hidden bypass + its audit table, isolated into its own pass given the sensitivity of a no-Stripe fee-waiver path.

**48hr follow-up policy — built and verified against real data (2026-08-14), closes the previously-open `awaiting_finalization` timeout question above.** A `customer_searches` row could sit in `awaiting_finalization` indefinitely before this — nothing acted on its age (confirmed via grep of `finalize-actions.ts`, `outreach-actions.ts`, `outreach-queue.ts`, and both cron routes — only `pending_refinement` age was ever checked, never `awaiting_finalization` age). **Single clock, anchored to `paid_at`, applies uniformly to original signups and switches** — both already set `paid_at` (switches via the Step 3b `p_paid_at` fix), so one query covers both origins with no per-origin branching. Triggers when a row is paid, has no `finalized_at`, and no `call_requested_at`, 48+ hours after `paid_at`. Excludes `search_status = 'switched'` — a superseded row means the customer already acted (by switching), not a real non-response. `getOverdueFollowUpQueue` (`src/lib/outreach-queue.ts`) surfaces as a new "Overdue follow-ups" section on `/internal/outreach`, positioned between "Switch calls requested" and "Active searches" (same pattern as the finalization/switch call queues) — make/model, customer email, an hours-overdue label, no action form yet (manual list for now, matching how the other queues started). To be replaced once the real agent dashboard (see `LEVR-Auto-Business-Plan-and-Roadmap.md`'s new "Three Dashboards" section) is designed.

- **Verified end-to-end against real data via a scratch test route** (same CRON_SECRET-gated pattern as every other verification pass this project) — 4 real `customer_searches` rows created under one real temporary auth user: a row paid 50h ago with no `finalized_at`/`call_requested_at` (should appear), a row paid 47h ago otherwise identical (should not — not yet past 48h), a row paid 50h ago but already finalized (should not — finalized), and a row paid 50h ago then switched via the real `switch_customer_search` RPC, not a manual status edit (should not — superseded). **All four matched expectations exactly**, including `fullQueueLength` confirming no false positives or negatives beyond the four explicit checks (i.e. the queue contained exactly the one expected row, not a coincidentally-correct subset). Test data (1 auth user, its `customers` row, 5 `customer_searches` rows including the post-switch row) cleaned up after via cleanup mode. Scratch route (`src/app/api/internal/scratch-overdue-followup-test/route.ts`) deleted after use, confirmed via grep no remaining references anywhere in `src/` — `api/internal/` again contains only the legitimate `sync-listings` route.
- **Real bug found and fixed during this pass, unrelated to the queue logic itself: `switch_customer_search` had two live overloads in production.** The `switch_fee_flow` migration's `create or replace function` added a 4th parameter (`p_paid_at`) — Postgres identifies a function by `(name, argument types)`, so adding a parameter creates a **new overload** rather than replacing the original 3-parameter function; both coexisted in the database. Any 3-argument call became ambiguous and errored instead of resolving, which is exactly what the scratch test's own `rowD` switch call (written as a 3-arg call) hit first. Confirmed the overload duplication directly via `pg_proc`/`pg_get_function_identity_arguments`, not inferred from the migration file alone. **Re-audited all three production call sites by reading the current code, not from memory — all three already passed `p_paid_at` explicitly on every call** (`switch-actions.ts`'s agent path, the Stripe webhook's `switch_fee` branch, `executeFreeSwitch`), so this was never a live production bug — only 3-arg callers (i.e. only the scratch test, since fixed) were ever affected. Fixed with an explicit `drop function public.switch_customer_search(uuid, text, text)` migration (`supabase/migrations/20260814150000_drop_switch_customer_search_3arg_overload.sql`) — run and verified: exactly one overload (the 4-parameter version) remains.

- **This finalized single-clock design supersedes an earlier two-trigger draft** (separate clocks for "never started self-service" vs. "started self-service, abandoned it") that turned out to need a schema change first — discovery found `FinalizeChoice`/`FinalizeSelfService` persist nothing to the DB until the customer's final "Confirm" click (all four steps — trim/color/options/review — are local React state only), so the two drafted triggers were indistinguishable in the data without a new `finalization_started_at` column. Collapsing to one clock anchored to `paid_at` sidesteps that gap entirely — no schema change needed. (The switch self-service flow, `SwitchChoice`, has the identical no-partial-persistence shape, confirmed by reading it, not assumed — moot for this policy since switching isn't one of its triggers, but the same constraint would apply to any future switch-side timeout.)
- **`search_status` exhaustively traced before building the query, not inferred.** Read the live `CHECK` constraint directly (`20260813180000_finalization_flow.sql`, unmodified by any later migration): 6 allowed values — `awaiting_finalization`, `pending_refinement`, `searching`, `paused`, `closed`, `switched`. Grepped every write to the column across `src/` and `supabase/migrations/`: only 4 are ever actually written — the column default (`awaiting_finalization`, on insert), `finalizeSelfService`/`finalizeSearchByAgent` (→ `pending_refinement`, always paired atomically with `finalized_at` in the same `.update()`), `solidifySearch` (→ `searching`, gated on already being `pending_refinement`), and the `switch_customer_search` RPC (old row → `switched`). `paused`/`closed` are written by nobody, anywhere, in any code path — legal per the schema but structurally unreachable today, presumably reserved for the not-yet-built admin views (build order item 12). Confirmed separately that `/internal/refunds-due` and `guarantee-assessment.ts` never touch `search_status` at all (only `guarantee_status`/`guarantee_resolved_at`) — resolving a refund has no bearing on this query. **Defensive exclusion approved**: `getOverdueFollowUpQueue` filters on `search_status = 'awaiting_finalization'` as an allow-list rather than excluding `switched`/`paused`/`closed` as a block-list — same result against every state reachable today, but fails closed (excludes) rather than open (includes) if a 7th value is ever added later without this query being revisited.

### Step 2 discovery (2026-08-14) — pre-payment inventory display

Discovery only, nothing built yet. Per the roadmap doc's Customer Journey step 1: "progressive intake filter (make → model → zip) with a live nationwide inventory count."

- **The live inventory count is still the Aug 1 static/fake UI — confirmed not wired to real data.** `src/lib/match-counter.ts` (unchanged since the `2026-08-01` initial landing-page commit, confirmed via `git log --follow`) is a hardcoded per-make table plus a seeded pseudo-random hash — it never queries `listings` or MarketCheck. `IntakeFilter`'s `MatchCounter` component (`src/components/intake-filter.tsx`) calls it directly client-side. The "N vehicles match right now" badge is fabricated for every combination, including ones with zero real synced inventory.
- **Intake no longer collects trim/color/options — confirmed correct, nothing to flag.** The `Vehicle` type in `intake-filter.tsx` and `IntakeVehicle` in `intake-actions.ts` are both make/model only; trim/color/options collection already lives entirely in `/finalize/[searchId]` post-payment, per the Aug 13 split.
- **Make/model + zip-radius count would be cheap on the current table as-is — no indexing or materialized view needed at current volumes.** `listings` already has a `(make, model)` index, and `syncListingsForMakeModel` caps every sync at 3 pages × 50 rows = 150 listings per make/model (`marketcheck-sync.ts`). A real count query is already a small, index-backed result set; radius filtering (see below) would run in application code against that small set, not against the whole table. This could need revisiting only if per-model sync volume caps are ever raised well past current levels.
- **No existing radius/geo pattern to reuse — would need to be built from scratch.** `listings` stores `dealer_zip` as plain text only, no lat/long columns, no PostGIS or earthdistance extension. MarketCheck's own search call (`src/lib/marketcheck.ts:searchActiveListings`) takes no location parameter at all — the sync is nationwide by make/model only, never radius-scoped. `match-counter.ts`'s use of `zip` is purely a seed for its fake multiplier, not a real distance calculation. A real radius count would need a zip-to-coordinate source (not currently in the codebase) plus a distance calc, most practically done in application code given the small per-model row counts above.
- **Surfacing, not deciding: what counts as "near."** No radius value is implied anywhere in code or docs — this needs an explicit decision before building.
- **Surfacing, not deciding: zero-match behavior is currently undefined by construction.** `estimateMatches` has a hard floor of `Math.max(3, ...)` — it can never return zero today, so there's no existing zero-listings UI state to preserve or reference; whatever real-data version replaces it will need to decide this fresh. `canSubmit` in `IntakeFilter` also doesn't currently gate on match count at all — a customer can proceed to payment regardless of what the badge shows.

### Step 3b discovery (2026-08-14) — $100 switch fee + 5-day grace period

Discovery only, nothing built yet. Per `LEVR-Auto-Core-Processes-v1.md` §1b: first 5 days after signup, one free switch (unadvertised, internal-only policy); after that (or for a second switch within the window), a $100 fee that resets both the Day 30 and Day 60 clocks.

- **`switch_customer_search` today is a pure DB make/model swap — no payment/Stripe involvement at all, by design, not oversight.** Current live version (`supabase/migrations/20260812130000_remove_package_size.sql`, which superseded the original `20260810120000_switch_customer_search_rpc.sql`): row-locks the old row (`for update`), rejects an already-switched row, inserts a new `customer_searches` row (`customer_id`, new `make`/`model`, old row's `zip` carried over — nothing else), then marks the old row `superseded_by_id` + `search_status = 'switched'`. `switch-actions.ts` (`switchCustomerSearch`, the Server Action wrapping the RPC) has an explicit doc comment already flagging the gap: "Deliberately does not implement the $100 switch fee / 5-day grace period... the new row starts unpaid (no guarantee clock) until it goes through the existing separate Stripe flow."
- **Switching does functionally clear the guarantee clock, but via full re-payment, not a $100 fee.** The new row's insert only sets `customer_id`/`make`/`model`/`zip` — every other column (`paid_at`, `finalized_at`, `solidified_at`, `search_status`, `guarantee_status`) lands at its column default (`null`/`null`/`null`/`'awaiting_finalization'`/`'pending'`). So the "resets Day 30/60 clocks" half of the Business Model promise does hold structurally — a switched search has zero stale timestamps carried over, and (per Step 1) the Day-30 job's `solidified_at`-anchored query structurally can't touch a row that hasn't been paid/finalized yet. But the mechanism for getting there today is the customer paying the full $699 again through the normal `createCheckoutSession` flow — there's no $100-fee path, and no code distinguishes "this is a switch, charge $100" from "this is a brand-new customer, charge $699."
- **No "signup date" column separate from `paid_at` exists — `customers.created_at` is the natural fit, not something new.** `customers` (`20260807230209_initial_schema.sql`) has `created_at timestamptz not null default now()`, set once at account creation (the same moment as auth signup, since a DB trigger creates the `customers` row from `auth.users`). This predates and is independent of any given search's `paid_at`, so it's already exactly "signup date" — a 5-day-grace-period check would read this, not a new column.
- **No `free_switch_used`-style flag or any switch-count tracking exists anywhere — confirmed via grep, not assumed.** Searched `src/` and `supabase/` for `free_switch`, `grace_period`, `switch_fee`, "grace period" — the only hit is the doc-comment sentence in `switch-actions.ts` quoted above (prose, not a flag). Nothing tracks whether a given customer has already used their one free grace-period switch.
- **Only one UI entry point calls this today, and it's agent-only — confirmed, not assumed.** Grepped for `switchCustomerSearch`/`switch_customer_search` callers: exactly `src/components/agent-switch-search-form.tsx` (renders `AgentSwitchSearchForm`, one per `search_status = 'searching'` card on `/internal/outreach`) → `src/lib/switch-actions.ts` (`switchCustomerSearch`, gated by `getAuthorizedAgent()`) → the RPC. No customer self-service path exists (removed intentionally back in the original switching build — see the "Switching make/model — agent-only" entry earlier in this file); the RPC is invoked from exactly one place in the whole app.

### Step 3b confirmation copy (2026-08-14, locked — approved, source of truth) — self-service switch UI + call-request queue

Three customer-facing screens. This is the approved, locked text — not a draft, do not revise without Brett re-opening it.

**A. Call-request confirmation:**
> Request received. Our team's been notified and will reach out to discuss your requested change as soon as possible. We're actively working deals for you and every other LEVR customer right now, so thanks for your patience -- you'll hear from us soon.

**B. Self-service, $100 case (outside grace period or already used):**
> Switching your make or model will:
> - Charge a $100 switch fee
> - Restart your 30-day and 60-day guarantee clocks from today
> - Not carry over any offer already found on your current vehicle -- the guarantee starts clean against your new pick

**C. Self-service, free grace-period case:**
> You're within your free switch window, so this won't cost anything. It will still restart your 30-day and 60-day guarantee clocks, and any offer already found on your current vehicle won't carry over.

### Step 3b UI discovery (2026-08-14) — self-service switch + call-request queue

Discovery only, nothing built yet.

1. **Finalization's self-service-vs-call choice — exact shape to reuse.** `/finalize/[searchId]/page.tsx` renders a single client component, `FinalizeChoice` (`src/components/finalize-choice.tsx`), with three internal modes (`"choice" | "self-service" | "call-requested"`) driven by local `useState`, seeded from `callAlreadyRequested` so a page reload after already requesting a call skips straight to the confirmation view. The `"choice"` mode shows two side-by-side cards ("Finalize it myself" / "Schedule a call"); picking self-service just flips local state to render `FinalizeSelfService` inline (no navigation); picking call fires a Server Action (`requestFinalizationCall`) and flips to a static green-check confirmation block. **Proposed reuse for switching:** the identical shape — one client component (`SwitchChoice`, say) with modes `"choice" | "self-service" | "call-requested"`, two cards ("Switch it myself" / "Have an agent handle it"), an already-requested seed prop, and the call path firing a new Server Action that sets `switch_call_requested_at` (mirrors `requestFinalizationCall`/`call_requested_at` exactly) before flipping to the locked call-request copy (screen A above). Not proposing new code yet, per your instruction — just confirming the pattern to copy.
2. **`/account` has no switch entry point today — would be genuinely new UI, not an extension of something already there.** Read the full page (`src/app/account/page.tsx`): each `SearchCard` renders status copy, a "Finalize this search" link when `awaiting_finalization` (`FinalizeEditForm` when `pending_refinement`), offers, add-ons, and post-acceptance deal-progress UI — nothing about switching make/model anywhere. This matches the earlier discovery finding that the customer-self-service switch UI was deliberately removed when switching became agent-only; a self-service entry point now would be adding it back, in a different (fee-aware) form, not restoring dead code.
3. **No "pending action awaiting payment" UI pattern exists on `/account` today, for either the original $699 or anything else — this needs to be designed from scratch, not reused.** Traced the original payment flow end to end: intake creates the `customer_searches` row immediately (`awaiting_finalization`, `paid_at: null`), then redirects straight to Stripe (`window.location.href = result.url` in `intake-filter.tsx`) — the customer never lands back on `/account` mid-checkout. `getCustomerDashboard` (`customer-dashboard.ts`) has no `paid_at` filter at all, so an *abandoned* original checkout would actually leave a real unpaid row visible on `/account` today — and it renders wrong: `SEARCH_STATUS_COPY['awaiting_finalization']` says "Payment received — finalize trim, color, and options," and `SearchCard` shows a live "Finalize this search" link with no `paid_at` check, which would dead-end at `/finalize`'s `if (!paid_at) redirect("/")` guard. **This is a pre-existing, unrelated gap** (not something this pass introduced or needs to fix) but it's the honest answer to "is there something to build on": no, there's a gap in the same spot, not a working pattern. A pending-switch-payment UI will need to be designed fresh — likely a `switch_requested_at IS NOT NULL` check on the `SearchCard`, showing "$100 switch to {pending_switch_make} {pending_switch_model} pending payment" with resume/cancel actions, analogous to but not copied from anything that exists today.
4. **Original $699 Checkout Session pattern, confirmed exact — the $100 switch-fee Checkout should mirror it.** `createCheckoutSession` (`src/lib/payment-actions.ts`): a Server Action taking a `searchId`, verifying the caller owns an unpaid row, building a Stripe Checkout Session via inline `price_data` (not a stored Price ID — consistent with the Step 3a finding), `metadata` (now including `type: "search_payment"`), `success_url` pointing at `/payment/success?session_id={CHECKOUT_SESSION_ID}`, `cancel_url` back to the homepage, returning `{ ok: true, url }` for the client to redirect to via `window.location.href`. The $100 switch-fee session (not built yet) should follow this identically — same ownership-verification-before-charging shape, same inline `price_data` (no stored Price ID), same `metadata` pattern but with `type: "switch_fee"` plus `old_search_id`/`new_make`/`new_model` (per the webhook's existing expectations, already built and verified), and a `success_url` that will need its own landing page (mirroring `/payment/success`'s job of confirming payment and handing off — though a switch has no `/finalize` to hand off to immediately, since the swap only happens on the webhook, not at Checkout redirect time).

### Step 2 zip-radius discovery/proposal pass (2026-08-14)

- **`dealer_zip` data-quality check — blocked, not yet run.** This session has no real Supabase credentials available (see the ⚠ note in the Day-30 tracking above — `.env.local` reads come back redacted, and the local dev server itself only has placeholder env values in this sandbox), so this couldn't be checked directly. Needs to be run manually before the `zip_coordinates` migration below is applied, to know whether `dealer_zip` needs cleanup (5-digit vs ZIP+4, nulls, non-US) before it's usable as a join key. Query to run in the Supabase SQL Editor:
  ```sql
  select
    count(*) as total,
    count(*) filter (where dealer_zip is null or dealer_zip = '') as null_or_empty,
    count(*) filter (where dealer_zip ~ '^[0-9]{5}$') as five_digit,
    count(*) filter (where dealer_zip ~ '^[0-9]{5}-[0-9]{4}$') as zip_plus_4,
    count(*) filter (where dealer_zip ~ '^[0-9]{9}$') as zip_plus_4_no_dash,
    count(*) filter (
      where dealer_zip is not null and dealer_zip <> ''
        and dealer_zip !~ '^[0-9]{5}$'
        and dealer_zip !~ '^[0-9]{5}-[0-9]{4}$'
        and dealer_zip !~ '^[0-9]{9}$'
    ) as other_format
  from public.listings;
  ```
- **`zip_coordinates` table is live in production and fully populated (2026-08-14)** — created via `supabase/migrations/20260814120000_zip_coordinates.sql` (`zip`/`latitude`/`longitude`/`created_at`, 5-digit check constraint, RLS enabled with an open `select` policy for anon+authenticated and no write policy — locked to service_role only, same convention as `listings`/`agents`). **33,791 rows imported** via the Table Editor CSV import from `supabase/zip_coordinates_import.csv` (gitignored, one-time artifact, not committed) — count confirmed, Guam/CNMI positive-longitude rows spot-checked correct.
  - Built from the real 2025 Census Bureau Gazetteer ZCTA National file (`https://www2.census.gov/geo/docs/maps-data/data/gazetteer/2025_Gazetteer/2025_Gaz_zcta_national.zip`, fetched and verified directly, not assumed from memory — its actual format turned out to differ from older Gazetteer years: pipe-delimited with an added `GEOIDFQ` column, not the plain tab-delimited layout prior years used). Header `zip,latitude,longitude`, `GEOID` kept as 5-digit text (leading zeros verified intact, e.g. `00601`), `INTPTLAT`/`INTPTLONG` mapped through as a **pure passthrough — no sign or value transform of any kind**, including the 10 Guam/Northern Mariana Islands rows whose longitude is genuinely positive in the source data (~+144 to +146 — Guam/CNMI are the only US zips in the Eastern Hemisphere). An initial version of this file stripped the source's explicit `+` prefix from those 10 rows for cosmetic consistency; the numeric value/sign was never altered by that (144.75 and +144.75 are the same value), but it was reverted anyway so the CSV matches the source file's literal formatting exactly. Re-verified against a **fresh independent re-download** of the source zip (same 952,026-byte file) via a full byte-for-byte `diff` of all 33,791 rows' zip/lat/long against a clean extraction straight from source — zero differences, proving no row anywhere (not just the flagged 10) had its sign or value altered. Also re-ran zero-duplicate-ZCTA, zero-empty-lat/long, and 5-digit-zip checks against the corrected file — all clean.
- **Query approach built and shipped** in `src/lib/inventory-count.ts` exactly as proposed: fetch `listings` filtered by `make`+`model` (index-backed), collect distinct `dealer_zip` values from that set plus the customer's own intake zip, look both up in `zip_coordinates` in one query, Haversine in application code (not PostGIS/earthdistance), filter to `INVENTORY_RADIUS_MILES` (`src/lib/inventory-radius.ts`). Listings whose `dealer_zip` doesn't resolve in `zip_coordinates` are excluded from the count rather than erroring — moot today (dealer_zip data confirmed 416/416 valid), but guards the future. Wired into `IntakeFilter`/`MatchCounter` (`src/components/intake-filter.tsx`) via a 400ms-debounced Server Action call — replaced the old synchronous fake `estimateMatches`. Zero-match state renders honestly ("0 matching listings tracked near you right now — our nationwide outreach can still source it"), same in all three places the count is shown (the live badge, the footer summary, and the post-submit confirmation screen); `canSubmit` still doesn't gate on match count. **Real end-to-end verification (2026-08-14):** zip `90210` against the 139 total synced Honda Civic listings → **8 genuine within-radius matches, 7.1–41.7 miles**; zip `99926` (Ketchikan, AK) against the same 139 listings → **genuine 0**, every listing correctly excluded at 1,200+ miles, not a fallback number. `match-counter.ts` (the old fabricated hardcoded-table-plus-hash logic) is fully deleted — confirmed via repo-wide grep, not just removed from the import graph — and the temporary diagnostic route used to run this test (`api/internal/scratch-inventory-test`) has been deleted.

## Pre-launch to-dos — don't forget these once there's real customer data

- **Preview deployments point at the same production Supabase project as live** (same DB, same auth users — no separate staging/test project exists yet). Fine for now since there's no real customer data, but this needs a proper split — a separate Supabase project for Preview, or branch-aware config — before real launch, so a test PR can never touch live customer data.
- **Stripe is in test mode everywhere** (`sk_test_...` / `whsec_...`, "LEVR Auto sandbox" account). No live keys configured anywhere — local, Preview, or Production. A real (test-mode) webhook endpoint is registered in the Stripe sandbox pointing at `https://levrauto.com/api/stripe/webhook` (`we_1U2cbg8FQdYEFttXePzCHvX8`, `checkout.session.completed` only), and `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` are set on both Vercel Production and Preview (Preview shares the endpoint/keys for now, consistent with Preview sharing the production Supabase project above). Before real launch: switch to live keys, and register a separate live-mode webhook endpoint (test-mode endpoints don't receive live events).

## Approach & patterns

- **Self-execution boundary for test/verification commands (decided 2026-08-15).** Claude Code may self-execute test/verification commands directly against Brett's live local dev server, but only when both of the following hold:
  - **The dev server is already running.** If it isn't, don't start it unprompted — ask first. Starting it is a state change outside what was requested, even though it's low-risk.
  - **Only non-secret-shaped credentials are used to authenticate** (e.g. `CRON_SECRET`, whose local-testing value doesn't pattern-match a real secret and so isn't redacted from tool output). Never attempt to read or use actual Stripe/Supabase secret values directly, even if they somehow became readable — those stay exclusively something Brett runs himself, same as every migration this project has ever used.
  - **Why this is safe:** Claude Code's Bash tool runs directly on Brett's own machine, in this project directory — there's no sandbox boundary between it and whatever Brett has running in another terminal. Hitting `localhost` from Bash is functionally identical to Brett typing the same curl command himself. What actually gates this is credential *readability*, not network access: `.env.local` reads come back redacted for values that pattern-match known secret formats (Stripe's `sk_`/`whsec_` prefixes, Supabase's JWT-shaped service-role key), so those can't be used even when the dev server is live and reachable. `CRON_SECRET` isn't redacted, which is what makes scratch/test-route verification (e.g. the extension-payment-flow pass, 2026-08-15) self-executable without Brett needing to run the curl commands himself.
  - **Scope: this covers scratch/test-route verification against local dev only.** It changes nothing about migrations, production data, or real payment/webhook actions against Stripe/Supabase directly — those still always go through Brett's own hands, exactly as before.
  - **Narrowed after a real incident (2026-08-15): this permission covers HTTP requests to the running dev server only — it does not extend to deleting or modifying files the live process depends on** (e.g. `.next`). A running Turbopack instance expects to own that directory continuously; deleting it mid-session (done here to clear a stale TypeScript error) wedged the live server into a state where it stopped responding to any request at all, including plain `GET /`, requiring Brett to restart it by hand. `.next` deletion in isolation is normally safe/disposable — the mistake was doing it without accounting for a live process already depending on it. **Standard going forward: any action that could disrupt the live server itself, not just query it, needs to be flagged before doing, not after** — the same bar test/verification HTTP requests are already held to, just missed once here.

- **Cross-channel messaging consistency (decided 2026-08-15).** Customer-facing copy must stay consistent in substance across every channel/format it appears in — email, dashboard banners, in-app confirmations — same core facts and tone, even when the exact wording adapts to fit the format (e.g. a banner uses a live countdown, an email uses a fixed date). Applies to all future customer-facing copy, not just Day-60 — when writing or reviewing copy for a flow that already has approved copy in another channel, check that channel first rather than drafting independently.

- **Key learnings & principles — `customers.email` is non-unique, denormalized display data (confirmed 2026-08-16).** Unlike `agents.email` (`text not null unique`, `initial_schema.sql`) or `auth.users.email` (uniqueness enforced separately by Supabase Auth itself), `customers.email` is just `text not null` — no constraint — and its own column comment already says it's a denormalized copy of intake/auth data kept for convenient reads (e.g. an agent-facing admin view) without joining into `auth.users`. Confirmed by reading the schema directly, not inferred. **Safe to write freely for test purposes** (e.g. a scratch route redirecting a test row's `customers.email` to a real inbox to verify a reminder email actually gets delivered) — **but never usable as a lookup key, in test or production code**, since two rows can legitimately (or accidentally, as in a test override) carry the same value. Confirmed via a full grep audit across `src/` that no production path does this today — the only `customers` lookup keyed by email anywhere was a scratch route's own cleanup logic, which broke exactly this way: a `setup` mode overrode a test row's `customers.email` to a real address for delivery testing, and the matching `cleanup` mode's by-email lookup then silently found nothing, leaving the row orphaned until a by-id fallback was added. **Lesson for future scratch routes:** if `setup` overrides a denormalized field like this, `cleanup` must key off the row's real `id`, never off the field that was overridden.

## Tech stack (decided)

- Next.js — app framework
- Supabase — Postgres DB, auth, storage
- Vercel — hosting
- MarketCheck API — dealer inventory data (live key exists, tested manually, not yet wired into the app)
- Stripe — payments
- ZeptoMail — transactional email (already set up via Zoho, same account as business email)
- Claude API — parsing freeform dealer reply emails into structured offers
- DocuSign or PandaDoc — e-signing

## Build order (do in this sequence)

1. ~~**Database schema first**~~ — **done**
2. ~~Auth (Supabase Auth)~~ — **done**
3. ~~Intake flow → writes to real DB~~ — **done**
4. ~~Payment (Stripe), flat $699~~ — **done, test mode**
5. ~~MarketCheck integration — single make/model sync, manually triggered~~ — **done**; ~~demand-driven scheduling (nightly for active make/models, weekly for everything else already known)~~ — **done, via Vercel Cron**. Monthly low-movement tier deferred until real usage data exists.
6. ~~Outreach engine — manual work queue (not automation): `/internal/outreach` + offer-logging form~~ — **done**. Actual dealer contact (call/email automation) is still **mechanism deliberately unproven, deferred** — don't over-build automation here yet; keep this manual/lightweight until real-world dealer response patterns are observed
7. Inbound reply parsing (Claude API) → populates `qualifying_offers`
8. ~~Dashboard (customer-facing) — built into `/account`, sets `delivered_at` on first view~~ — **done**
9. ~~Change-request logic — accept/decline, the sold-before-response guarantee edge case, switching make/model, the Day-30 guarantee assessment job, and add-on negotiation~~ — **done**. The $100 switch fee/grace-period gating is a separate, still-unbuilt piece of this same build-order item (see "Not built yet" above)
10. ~~Financing/document flow — availability re-confirmation, deposit recording, financing capture~~ — **done**. The LEVR e-sign document (PandaDoc) is a separate, still-unbuilt piece of this same build-order item, blocked on a sandbox API key (see "Not built yet" above)
11. Delivery coordination
12. Minimal admin views

## Critical schema decisions already made — implement these correctly from the start

**`listings` vs `qualifying_offers` — these must be separate tables, not one.**
- `listings` = raw MarketCheck data (VIN, dealer, price, MSRP, trim, synced_at). Just sourced inventory, refreshed on a cadence.
- `qualifying_offers` = ONLY populated when a dealer actually responds to outreach with a real, itemized offer they're willing to honor. **An advertised listing price is never automatically a Qualifying Offer** — this is a hard business rule, not a UI nuance. Only rows in this table count toward the 30-day guarantee.

**`customer_searches` table doubles as the demand registry.** Drives MarketCheck sync cadence:
- Any make/model with an active row here → sync nightly
- Everything else → weekly, or monthly for consistently low-movement makes/models
- Rationale: most of the national inventory has zero active customers on any given day; pulling all of it nightly wastes API budget for no product benefit

**`assigned_agent_id` on every customer case, from day one** — even though right now it only ever points to Brett. This is what guarantees continuity (same agent stays with a customer) once there's ever a second person on the team. Cheap to build in now, annoying to retrofit later.

**VIN is the de-duplication key** for `listings` — upsert on VIN, never insert-always, or re-syncs create duplicate rows.

**Every MarketCheck sync call needs an explicit `car_type=new` filter** — testing showed the API defaults to mostly used inventory without it.

## The guarantee — exact rule (already decided, implement precisely)

- MSRP = Monroney label total (base + factory options + destination), excludes tax/title/doc/dealer add-ons
- Qualifying Offer = any dealer offer below that MSRP on a matching vehicle
- Assessed at Day 30 from solidification — not payment. The customer has a 24h post-payment refinement window to fine-tune trim/color/options; the clock starts once that's locked in (`solidified_at`), not at the moment of payment (`paid_at`). Changed 2026-08-12 — see the Day-30/solidify entries below.
- **Sold-to-someone-else edge case:** customer gets a 24-hour response window from when a Qualifying Offer is delivered. No response within 24hrs + car sells → offer still counts (guarantee satisfied). Customer responds within 24hrs but car sells before it goes through anyway → doesn't count, LEVR keeps searching.
- Free search continues through Day 60 either way. After Day 60: $100 per ~30-day extension.

## Known open problems — don't design around false assumptions

**Outreach mechanism is unproven.** MarketCheck gives dealer phone + website, NOT email. ADF/XML lead delivery (the "real" scalable path) typically requires being a recognized lead vendor with the dealer's specific CRM system — a business relationship, not just an API call. Don't build heavy automation on an assumed email-outreach flow. This has been deliberately deferred as a business decision, not an oversight — revisit once real transactions are running.

**MarketCheck has no single-query nationwide radius.** Every tier caps a single search to 100-500 miles from one point. Genuine nationwide coverage requires running multiple searches anchored at different regional centers and stitching/deduping results (by VIN).

**MarketCheck pricing:** Free tier (500 calls/mo) currently in use for testing. Realistic production tier is ~$749/mo + an unspecified usage-based "data fee" — confirm exact numbers once their sales team replies with a real quote.

## Brand assets

- Logo files: `levr-auto-logo-final.png`, `levr-holdings-llc-logo-final.png` — both have real transparency, already in site header/footer/favicon
- Primary color: `#1746A2`
- Tagline: "Car buying, with the leverage on your side."

## Things NOT to build yet (explicitly deferred, don't scope-creep into these)

- Dealer subscription/lead-routing marketplace (Stage 4 — needs real transaction volume first)
- In-house review video content
- AI voice-calling for dealer outreach (real open question, needs legal review before any real use)
- Post-purchase mailer/giveaway program (has an unresolved sweepstakes-law question — needs attorney review before it's real)
- Lender referral, transporter marketplace revenue streams
