# LEVR Auto — Project Context for Claude Code

This file exists so any Claude Code session (yours, your collaborator's, or a future one) has full context without re-explaining the business. Read this before starting build work.

## What this business does

LEVR Auto negotiates new-car purchases on behalf of consumer buyers, nationwide. Customer picks a make/model (up to 3), pays an upfront fee, LEVR sources matching dealer inventory and negotiates by phone/email, offers land in a dashboard, customer accepts one. Money-back guarantee if no below-MSRP offer within 30 days.

Legal entity: **LEVR Holdings LLC** (Kansas). Public brand: **LEVR Auto**.

Full business logic lives in `LEVR-Auto-Core-Processes-v1.md` and `LEVR-Auto-Business-Plan-and-Roadmap.md` in this repo — read those for anything not covered here.

## Current state (updated 2026-08-12 — keep this current, it's the first thing read each session)

- **Live site:** levrauto.com (Next.js + Tailwind, deployed on Vercel, GitHub repo `brettjohnston80/levr-auto`)
- **Built and live:**
  - Database schema — `agents`, `customers`, `customer_searches`, `listings`, `qualifying_offers`, RLS on all five (`supabase/migrations/`)
  - Auth — Supabase Auth email/password sign-up + login (`/login`, `/signup`, `/account`), a DB trigger auto-creates the matching `customers` row on signup. Custom SMTP (ZeptoMail, distinct SMTP credentials from the API key already in use) is configured in Supabase Auth settings — Supabase's default email sender only delivers to Supabase org team members, which was silently blocking password reset and would have blocked real customer signup confirmations too. Verified with a real password reset email sent to `bjohnston@levrauto.com` and confirmed delivered to the actual inbox.
  - Password reset landing page — `/auth/reset-password` catches the incoming recovery link and lets the user set a new password (`updatePasswordFromRecovery`, `src/lib/auth-actions.ts`), then signs them out and redirects to `/login`. Root cause of why this was missing/broken: **this project's Auth email templates use the implicit flow**, not PKCE — recovery links redirect back with `access_token`/`refresh_token` in the URL hash fragment, which is client-only and never reaches the server. The existing `/auth/callback` route (server-side, expects a `?code=` query param to exchange) can never see a hash fragment, so pointing recovery links there — the natural first instinct — silently fails and dumps the user on `/login` with no session. The fix: `/auth/reset-password` is a client component that parses `window.location.hash` itself and calls `supabase.auth.setSession()` client-side to establish the session, then shows the form. Verified end-to-end: a real recovery link (via `admin.generateLink`, same token format `resetPasswordForEmail` produces) was opened in a real browser, correctly landed on the form, a new password was set, the app signed the session out and redirected to `/login` with a confirmation message, and logging back in with the new password succeeded — genuinely proving the new password works, not just that the form submits. Also confirmed a stale/missing link correctly shows an error instead of a broken form.
  - `/auth/callback` fixed with the same approach — was a server route (`route.ts`, `exchangeCodeForSession`), now a client page (`page.tsx`) that parses the hash fragment and calls `setSession()`, same as reset-password. This one was directly confirmed broken, not just suspected: a **genuine signup** through the real `/signup` UI (not the admin-API bypass every earlier test used) sent a real confirmation email via ZeptoMail, and clicking the equivalent real confirmation link landed on `/login?error=Could not verify your email` with the tokens visibly stuck in the URL hash — while Supabase's own side had already marked the email confirmed. So the account was actually usable, but the app told the user it wasn't and dropped their session, on every real signup. After the fix, a second genuine signup + real confirmation link landed cleanly on `/account`, fully signed in. If any other flow ever points `emailRedirectTo`/`redirectTo` at `/auth/callback` or `/auth/reset-password`, it'll work the same way — both now expect hash-fragment tokens, matching this project's actual email template configuration.
  - `/forgot-password` — was missing entirely (no way to trigger a reset email from the UI, only tested via direct `resetPasswordForEmail` calls/scripts so far). Simple email-only form (`requestPasswordReset`, `src/lib/auth-actions.ts`) that calls `resetPasswordForEmail` with `redirectTo` pointed at `/auth/reset-password`, and always shows the same generic "if an account exists…" message regardless of whether the email is real, to avoid account enumeration. Linked from `/login` via a "Forgot password?" link next to the password field. `/login` also gained `message` query param rendering (previously only `error` rendered, which would have silently swallowed this flow's own success message). Verified with the real form end-to-end, not scripted: submitted a real email on the real page, triggered a real `resetPasswordForEmail` call, followed the resulting link through `/auth/reset-password`, set a new password, and logged in with it successfully — the complete real user path with no admin-API shortcuts.
  - `/auth/confirm` — new intermediate landing page, added after real (post-Site-URL-fix) password reset links started showing "invalid or expired" for Brett despite the mechanism itself testing clean. Diagnosed by isolating variables: a token generated and clicked immediately (bypassing email) worked every time; a token sent through real email and clicked by Brett failed every time. Root cause: Supabase's own `/auth/v1/verify` endpoint consumes a single-use token on its very first `GET` — so *any* automated fetch of the link (an inbox security scanner pre-checking URLs before delivery, a link-preview generator, anything without a real person behind it) silently burns the token before the real user ever clicks it. This isn't specific to Zoho/ZeptoMail or this inbox — any customer's own mail security could do the same thing, so it needed a fix that doesn't depend on the sending or receiving mail environment. Fix: Supabase's email templates now point at `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=recovery` (or `type=signup`) instead of `{{ .ConfirmationURL }}` — a page on our own domain that renders inert (token sitting unused in the URL) until an explicit button click calls `verifyOtp({ token_hash, type })`. A mere fetch of the URL only loads a static page; nothing about that consumes the token, since verifyOtp only ever fires from the click handler. `/auth/reset-password` now checks for an existing session first (the case once `/auth/confirm` hands off) before falling back to its original hash-fragment parsing, which still works for direct Supabase links. Verified rigorously: simulated the exact failure mode with a plain `curl` GET of the confirm URL (no JS execution, mimicking a scanner) confirmed the page still worked afterward for a real click — proving the fetch alone doesn't consume anything — then completed the full loop (click → password set → login with new password) on production for both `type=recovery` and `type=signup`, plus confirmed a link with no token shows a clean invalid-link state. The Supabase email template change itself is a dashboard edit outside API access available in this session — Brett applied it directly.
  - One more real bug found immediately after the template edit: the actual first real reset email 404'd. The resolved link was `https://levrauto.com/**/auth/confirm?...` — a literal `/**` before the path. Cause: Supabase's **Site URL** field (not the email template, which was correct) had been set to `https://levrauto.com/**` — the wildcard syntax belongs in the separate Redirect URLs allow-list, not Site URL, which must be the bare domain. Diagnosed from the actual resolved email link (not the template source, which looks identical either way) — Brett copied the real `href` out of the received email, which is what surfaced the extra `/**`. Fixed by correcting Site URL to plain `https://levrauto.com`. Confirmed fixed with a brand-new real email after the correction, not a manually-patched URL.
  - **Both flows now verified end-to-end under genuinely real conditions** — real signup and real password reset, through real emails, clicked by Brett on his own real inboxes (`bjohnston@levrauto.com` for reset, `brettjohnston80@yahoo.com` for signup, chosen specifically to test a real address outside the Zoho/ZeptoMail ecosystem this was all triggered by), landing correctly on `/auth/confirm` → real click → real session → real password set / real account signed in. This is the strongest verification standard used all night — no `admin.generateLink` shortcuts, no bypassing email delivery. Test signup account deleted after.
  - Intake flow → real DB — the homepage intake filter (make/model/trim/color/zip) writes real `customer_searches` rows. Not signed in? An inline modal prompts login/signup right at the "Continue" click (not before) — no separate nav entry point exists for this on purpose. Rows land at `search_status = 'pending_refinement'` / `guarantee_status = 'pending'` / `paid_at = null` by default.
  - Payment — Stripe Checkout (test mode), tied to the 3 tiers. After saving the intake rows, "Proceed to Payment" creates a Checkout Session (via `createCheckoutSession`, `src/lib/payment-actions.ts`) and redirects to Stripe's hosted page. On `checkout.session.completed` (`src/app/api/stripe/webhook/route.ts`), the webhook sets `paid_at` and `stripe_checkout_session_id` on the paid `customer_searches` row(s) — this is the Day-30/Day-60 guarantee clock anchor. Verified end-to-end with a real test-mode payment and a real webhook delivery (via `stripe listen`), including a replay test proving the `paid_at IS NULL` guard makes it idempotent. Deliberately does **not** touch `search_status` — that only moves to `'searching'` once the 24h post-payment refinement window closes, which is separate, unbuilt logic. Live keys not configured anywhere — test mode only until the business is actually ready to take real money.
  - `/matchmaker` page — still front-end only, mock data, not wired to the DB
  - MarketCheck sync, core (single make/model) — `syncListingsForMakeModel` (`src/lib/marketcheck-sync.ts`) calls the MarketCheck active-listings search (`src/lib/marketcheck.ts`, always with an explicit `car_type=new`) and upserts into `listings` on `vin`, paginated (50/page, capped at 3 pages per call for now — MarketCheck free tier is 500 calls/mo and a popular model can have 10k+ national listings). `car_type=new` alone isn't fully reliable (confirmed against real data — MarketCheck still lets through older model years, e.g. a 2019 model tagged `car_type: new`), so there's a second trust check: a listing is only upserted if its model year is the current year or one year ahead (`isTrustworthyNewListingYear`); anything older or missing a year is excluded and counted in the sync response as `excludedForYear`. Manually triggerable via `POST /api/internal/sync-listings` (`{ make, model }` body) for ad-hoc single-model syncs/backfills.
  - MarketCheck sync, demand-driven scheduling — `src/lib/marketcheck-scheduler.ts`: `getNightlyMakeModels()` (distinct make/model from `customer_searches` where `search_status = 'searching'`) and `getWeeklyMakeModels()` (distinct make/model already known via `listings`, minus whatever's in the nightly set). `runBatchSync()` syncs a list of make/models sequentially, continuing past individual failures. Two Vercel Cron targets (`vercel.json`): `/api/cron/sync-listings-nightly` (`0 6 * * *`) and `/api/cron/sync-listings-weekly` (`0 7 * * 0`), both GET, both authenticated via `Authorization: Bearer $CRON_SECRET` (Vercel sends this automatically on cron-triggered requests once `CRON_SECRET` is a project env var — now set on Vercel Production and Preview). The "monthly for consistently low-movement makes/models" tier from the original strategy is deliberately not built yet — needs real usage data to define "low-movement," which doesn't exist with zero real customers. Verified end-to-end against real data: nightly correctly empty with no active searches, weekly correctly synced the two make/models already in `listings` (Civic, Camry), and — using a real temporary test customer/search — nightly correctly picked up a newly-`searching` make/model (Ford Mustang) while weekly correctly excluded it (diffed against the just-updated nightly set). Test data cleaned up after.
  - All authenticated routes reject requests with a missing/wrong `CRON_SECRET` (verified: 401).
  - Outreach queue (manual work tool, not automation) — `/internal/outreach`, gated by `requireAgent()` (`src/lib/agent-auth.ts`): reuses the existing customer-facing Supabase Auth session (no separate agent login system), a signed-in user only counts as staff if their email matches an active row in `agents`. Brett's real agent login now exists (`bjohnston@levrauto.com`, linked to the pre-existing `agents` row) — note signing in via the normal `/login` form also auto-creates a `customers` row for that email via the existing trigger, a harmless accepted side effect of reusing customer auth. The page (`src/lib/outreach-queue.ts`) lists `customer_searches` at `search_status = 'searching'`, each with dealers pulled from `listings` by exact make/model match (aggregated to distinct dealers, sorted by inventory count — no zip/radius filtering) and any `qualifying_offers` already logged. An inline expandable form per search (`src/components/log-offer-form.tsx`) logs a real dealer offer via the `logQualifyingOffer` Server Action (`src/lib/outreach-actions.ts`), which re-checks agent auth server-side and writes through the admin client (mirrors the MarketCheck sync writes — `qualifying_offers` has no client-facing insert policy). Deliberately leaves `delivered_at` and `status` at their defaults (`null`/`'pending'`) — those belong to the customer dashboard/24h-response-window flow, not built yet. No automated calling/emailing — purely a human work queue. Verified end-to-end via real browser login as Brett's actual agent account: real matching dealers rendered correctly for a temporary test search, a real below-MSRP offer was logged through the actual form and confirmed correct in the DB (`is_below_msrp` computed `true`), and all three auth-gate cases were confirmed (unauthenticated → `/login`, signed-in-non-agent → `/`, signed-in-agent → the page). Test data cleaned up after; Brett's own login's temporary test password was rotated to an unknown value — use "Forgot password" to set a real one.
  - Customer dashboard — built into `/account` in place (replaced the old bare "you're signed in" debug view; no redirect targets needed to change since login/signup already send users there). Lists all of the signed-in customer's `customer_searches` (not just `search_status = 'searching'` — a `pending_refinement` search shows explanatory copy about the 24h window instead of disappearing), each with its `qualifying_offers` (dealer name, price, MSRP, a below-MSRP badge, status, delivery time). Deliberately doesn't show `dealer_contact` or `notes` — internal-only fields, not written for customer eyes. The real logic: `getCustomerDashboard` (`src/lib/customer-dashboard.ts`) sets `delivered_at` on any offer that doesn't have it yet, at the moment it's loaded here — per the schema's own comment, `delivered_at` is the dashboard-appearance moment, not raw dealer receipt, and it's what starts the guarantee's 24h response-window clock. The write is guarded by `WHERE delivered_at IS NULL`, so it's naturally idempotent — revisiting the page, or a concurrent load, can't re-fire or double-set it. Verified with real data: a temporary customer with a `searching` search and an offer at `delivered_at = null` — first page load rendered it correctly and set `delivered_at` for real in the DB (confirmed via direct query); reloading the page left `delivered_at`/`updated_at` byte-identical, proving the idempotency guard. Test data cleaned up after.
  - Change-request logic — three pieces, all in `/account` (customer-facing) and `/internal/outreach` (agent-facing):
    - **Accept/decline** — `respondToOffer` (`src/lib/offer-response-actions.ts`), buttons rendered by `OfferResponseButtons` on any offer at `status = 'pending'`. Sets `customer_responded_at` and `status` (`customer_accepted`/`customer_declined`), guarded by a `.eq("status", "pending")` write condition (not just a pre-check) so a double-click or race can't overwrite an already-recorded response.
    - **Sold-before-response guarantee edge case** — `evaluateOfferGuaranteeContribution` (`src/lib/guarantee.ts`) is a pure, read-time derivation (deliberately not a stored column, per the schema comment on `qualifying_offers`): below-MSRP + delivered + never sold → `'counts'`; sold and the customer responded inside the 24h window from `delivered_at` → `'does_not_count'`; sold and the customer did not respond inside that window → `'counts'`. `vehicle_sold_at` itself is set by an agent via `markOfferVehicleSold` (`src/lib/outreach-actions.ts`), surfaced as a "Mark vehicle sold" button in `/internal/outreach` on below-MSRP offers only, guarded by `.is("vehicle_sold_at", null)`.
    - **Switching make/model — agent-only, not customer self-service.** `switchCustomerSearch` (`src/lib/switch-actions.ts`) requires `getAuthorizedAgent()` (the same gate as every other `/internal/outreach` action) and calls a Postgres function, `switch_customer_search` (`supabase/migrations/20260810120000_switch_customer_search_rpc.sql`), that inserts the new row and marks the old one (`superseded_by_id`, `search_status = 'switched'`) atomically in one transaction, row-locked (`for update`) so two concurrent switch attempts on the same search can't both succeed — never a silent edit. Trim/colors/required_options reset to blank on the new row (they're model-specific); `zip`/`package_size` carry over; `paid_at`/`solidified_at`/`guarantee_status` reset to defaults, so the new row starts as a fresh, unpaid engagement. Deliberately does **not** implement the $100 switch fee or 5-day free-grace-period check from `LEVR-Auto-Core-Processes-v1.md` §1b — out of scope for this pass, flagged explicitly rather than silently skipped.
      - **This changed from customer self-service to agent-only** — the original `SwitchSearchForm` (rendered on `/account`, gated on the *customer's own* session) was removed entirely and deleted from the codebase; `switchCustomerSearch` was rewired from a customer-ownership check to an agent-auth check. The only way to switch a search now is `AgentSwitchSearchForm` (`src/components/agent-switch-search-form.tsx`) on `/internal/outreach`, one per active (`search_status = 'searching'`) search card — an agent runs it after the customer requests a change by phone/email. A switched or not-yet-searching search naturally has no switch control on either page: it drops out of the outreach queue's `search_status = 'searching'` filter, and `/account` never rendered a control for any status besides the switch UI that no longer exists.
      - In its place, `/account` has a new **`AccountFaqSection`** (`src/components/account-faq-section.tsx`) — a collapsible "Need to change your search?" panel, customer-account-only (not the public `/faq` page), explaining that switching is agent-handled, with a `mailto:support@levrauto.com` link (subject + body pre-filled, including the customer's account email) that opens the customer's own mail client. `support@levrauto.com` is not yet a confirmed/provisioned inbox — same caveat as any other pre-launch contact address, needs a real mailbox before this is customer-facing in production.
    - Verified end-to-end in a real browser against real (temporary) data: logged in as a real test customer via a real Supabase session (magic-link token consumed through the real `/auth/confirm` click-through, same mechanism as production auth), clicked Accept on one offer and Decline on another and confirmed both persisted correctly with the right timestamp. Separately logged in as a real test agent via `/internal/outreach`, clicked "Mark vehicle sold" on two different offers, and ran `evaluateOfferGuaranteeContribution` against the real resulting DB rows: an offer delivered 30h earlier with no response, then sold, correctly returned `'counts'`; an offer accepted within a minute of delivery, then sold, correctly returned `'does_not_count'`. Test data (2 auth users, 2 `customer_searches` rows, 3 `qualifying_offers`, 1 `agents` row) cleaned up after.
    - **Switch-gate re-verified separately** (2026-08-12), after moving switching to agent-only: a real test customer's `/account` page showed no switch control anywhere, and the new FAQ section rendered and expanded correctly with a working `mailto:` link. A real test agent then used `AgentSwitchSearchForm` on `/internal/outreach` to switch that customer's Honda Accord search to a Toyota Camry — confirmed directly in the DB that the old row flipped to `search_status = 'switched'` with `superseded_by_id` pointing at a real new row carrying the correct fresh-engagement defaults (`paid_at: null`, `guarantee_status: 'pending'`, `search_status: 'pending_refinement'`). Reloading `/account` as the customer showed both rows correctly (old marked "Superseded by a newer search", new as pending refinement), still with no switch control on either. Test data cleaned up after.
  - Day-30 guarantee assessment job — `/api/cron/day30-guarantee-assessment`, same Vercel Cron + `Authorization: Bearer $CRON_SECRET` pattern as the MarketCheck sync jobs (`vercel.json`, daily at `0 8 * * *`). `getDueSearches` (`src/lib/guarantee-assessment.ts`) finds every `customer_searches` row with `guarantee_status = 'pending'` and `paid_at <= now() - 30 days` — deliberately an *at-least*-30-days-elapsed check, not an exact-Day-30-only check, specifically so a missed run (outage, transient error) can't create a permanent gap: an unresolved row just gets caught by the next day's run instead of falling through. The `guarantee_status = 'pending'` condition (checked again on the write, not just the read) is what actually makes this idempotent — a row only appears once, ever, whichever run happens to catch it. Deliberately has no `search_status` filter: the guarantee is anchored to `paid_at`, not to whether the search is still actively `'searching'`, so a row later marked `'switched'` still gets its own Day-30 resolution based on whatever offers arrived on it before the switch. `assessSearch` loads the row's `qualifying_offers`, runs the existing `evaluateOfferGuaranteeContribution` (unmodified) on each, and sets `guarantee_status = 'met'` if any evaluates to `'counts'`, else `'refunded'`. The `'refunded'` case only ever sets that status — it never calls Stripe. `guarantee_status = 'refunded'` is itself the worklist for a human to go process the actual refund. Verified against real (temporary) data covering six cases hit via the real HTTP route, not just the lib function directly: a 35-days-paid row with zero offers → `refunded`; a 30-days-paid row with a below-MSRP delivered offer never sold → `met`; a 31-days-paid row whose only offer was above MSRP → `refunded`; a 10-days-paid row (not due yet) → correctly untouched/still `pending`; a 40-days-paid row already at `guarantee_status = 'met'` → correctly untouched (proves the pending guard); a 32-days-paid `'switched'`-status row with a qualifying offer → `met` (proves no `search_status` dependency). Also confirmed a second immediate run assessed zero rows (idempotency) and that the route 401s without the correct `CRON_SECRET`. Test data cleaned up after.
  - Refunds Due — `/internal/refunds-due`, same `requireAgent()` gate as `/internal/outreach`. Read-only worklist (`getRefundsDueQueue`, `src/lib/refunds-due.ts`) of every `customer_searches` row at `guarantee_status = 'refunded'`, joined to the owning customer's name/email, showing make/model/trim and a "resolved" timestamp. No mark-as-processed action — the actual Stripe refund stays a deliberately separate, manual step, same as the Day-30 job above. The "resolved" timestamp is `guarantee_resolved_at` (`supabase/migrations/20260811090000_guarantee_resolved_at.sql`), a dedicated column set only by `assessSearch` (`src/lib/guarantee-assessment.ts`) at the exact moment `guarantee_status` transitions from `pending` — not `updated_at`, which every write path on the row touches and would drift the instant something unrelated (e.g. a post-resolution switch-make/model request) updated the row again. Verified end-to-end in a real browser as a real test agent against real (temporary) data: a `'refunded'` row rendered correctly with the right customer name/email/search/timestamp, while sibling `'pending'` and `'met'` rows for the same customer were correctly excluded; unauthenticated access correctly redirected to `/login`. Separately proved the drift-immunity directly: after the Day-30 job set `guarantee_resolved_at`, an unrelated write to the same row (a `notes` edit) bumped `updated_at` as expected but left `guarantee_resolved_at` byte-identical. Test data cleaned up after. Intentionally small — the fuller admin views (build-order item 12) are still a separate, later piece.
  - Add-on negotiation — the last piece of change-request logic. New `offer_addons` table (`supabase/migrations/20260811100000_offer_addons.sql`): itemized fees on a `qualifying_offers` row (`description`, `amount_cents`), added by an agent any time after the offer itself is logged (not forced into the initial `logQualifyingOffer` form — dealers often only reveal a fee breakdown once actually asked). This also closes a pre-existing documented gap: `LEVR-Auto-Core-Processes-v1.md` already said dealer offers must be itemized and the dashboard should show add-ons, but `qualifying_offers` only ever stored a total price.
    - Customer flow: `requestAddonRemoval` (`src/lib/addon-actions.ts`) flags a specific add-on for removal — routes to the agent via the outreach queue, never automated. Surfaced on `/account` (`AddonRemovalButton`) next to each add-on. Ownership re-checked server-side through `offer_addons → qualifying_offers → customer_searches.customer_id`, same pattern as `respondToOffer`.
    - Agent flow: `logOfferAddon` (itemize a fee) and `resolveAddonRemoval` (record what the dealer said: accepted/declined/countered, plus an optional note) in `outreach-actions.ts`, surfaced on `/internal/outreach` via `AddOfferAddonForm` and `ResolveAddonRemovalForm`.
    - **Deliberate scope decision, not an oversight:** `removal_status`/`dealer_response`/timestamps live directly on the `offer_addons` row — one active/most-recent negotiation round per add-on, not a full history thread. A customer can re-request after `dealer_declined`/`dealer_countered` (a new round), which overwrites the prior round's `dealer_response` and timestamps; only the latest round is ever visible. This doesn't block adding a proper rounds table later — that's additive (a new table + an extra insert alongside the existing update, no rework of `offer_addons`'s existing columns or the pages/queries that read them) — but it does mean any round that already happened and was already overwritten before a rounds table existed can never be reconstructed. Revisit if real usage shows agents actually need prior-round history.
    - Verified end-to-end in a real browser, sequentially as a real test agent and a real test customer (two simultaneous sessions aren't possible — Supabase Auth cookies are shared per-browser-profile across tabs, so concurrent logins in separate tabs silently clobber each other's session): agent logged a real add-on, customer saw it and requested removal (confirmed `removal_status = 'pending'` in the DB), agent resolved it as declined with a note (confirmed `dealer_response` and `removal_resolved_at` set), customer saw the decline reason and the "Request removal" button reappear, customer re-requested (round 2) — confirmed in the DB that `removal_status` reset to `'pending'`, `dealer_response` reset to `null`, and `removal_requested_at` advanced to a new, later timestamp. Test data cleaned up after.
  - Communication preferences, captured at signup — two new `customers` columns (`supabase/migrations/20260811110000_communication_preferences.sql`): `communication_frequency` (`'real_time'` / `'daily_digest'`, default `'real_time'`) and `communication_channel` (`'text'` / `'email'` / `'agent_callback'`, default `'email'`). Deliberately **capture-only**: no notification-sending system reads these yet — texts/emails actually going out on a real-time or digest cadence is a separate, later build. Threaded through the existing `handle_new_user()` DB trigger the same way `full_name`/`phone` already were, with `coalesce(..., 'real_time')` / `coalesce(..., 'email')` fallbacks in the trigger so any signup path that omits a field still gets a sane default. Both `text` and `agent_callback` need a real phone number to be usable (a personal agent can't call back with no number, same as a text can't send with no number) — `channelRequiresPhone()` (`src/lib/communication-preferences.ts`, a plain module with no `"use server"`, since `auth-actions.ts` requires every export to be an async Server Action and can't hold a sync helper) is the single shared source of truth for that rule, enforced server-side in both `signup()` and `signupInline()` (`src/lib/auth-actions.ts`).
    - **Both signup entry points now collect channel** (not just `/signup`) — the compact `AuthGateModal` used mid-intake-flow gained a channel `<select>` (default Email, zero added friction for anyone who doesn't touch it) and a phone field that only appears when `text`/`agent_callback` is chosen, mirroring `/signup`. **Frequency stays out of the modal on purpose** — per the design discussion, it doesn't gate any other field the way channel gates phone, and can reasonably wait for a future account-settings page; it simply isn't sent from `signupInline`, so it defaults to `'real_time'` via the same trigger fallback used everywhere else.
    - **Bug fix found and closed in the same pass:** `/signup`'s original phone-required check only covered `channel === 'text'`, not `agent_callback` — a real gap, since a callback is equally uncollectable without a number. Both `/signup` and the modal now use the same `channelRequiresPhone()` check.
    - Verified end-to-end with real signups through both real UI paths (not shortcuts) — the modal was driven through the actual homepage intake flow (make/model/zip → Continue → auth gate), not a direct navigation: `text` with no phone was correctly blocked (native HTML `required` validation, confirmed via DB check that no account was created — this made the client-side JS error banner redundant in this exact path since native validation intercepts first, but the outcome is what matters); `agent_callback` with no phone was also correctly blocked (proving the bug-fix path in the modal too); `text`+phone and `agent_callback`+phone both produced correct `customers` rows (right channel, right phone, frequency defaulted to `'real_time'`); the untouched-default path (`email`, no phone field ever shown) produced a correct row with `phone = null`. Separately confirmed `/signup`'s fixed guard: submitting `agent_callback` with no phone (that field has no HTML `required` there, so this hits the real server-side check, not native validation) correctly redirected with a visible error and created no account. Test data cleaned up after.
  - Financing/document flow, non-blocked pieces only — availability re-confirmation, deposit recording, and financing capture (Build order item 10). The e-sign piece (PandaDoc) is a separate, later addition once a sandbox API key exists — see "Not built yet" below.
    - **Deposit is record-keeping only, never a LEVR-processed payment.** The reservation deposit goes directly from customer to dealer; LEVR never touches this money. `deal_progress.deposit_amount_cents` is whatever the agent is told the dealer collected (varies per deal, not a LEVR-set fee), and `deposit_confirmed_at` is set manually by the agent once the dealer confirms receipt — same pattern as `qualifying_offers.vehicle_sold_at` (a nullable timestamp *is* the boolean; no separate confirmed flag alongside it). No Stripe Checkout Session, no webhook, no refund-execution logic for this piece.
    - New `deal_progress` table (`supabase/migrations/20260811120000_deal_progress.sql`), 1:1 via unique FK to `qualifying_offers`, created lazily by whichever agent/customer action fires first (upsert on `qualifying_offer_id`, so `confirmAvailability`/`confirmDepositReceived`/`submitFinancingChoice` can happen in any order without clobbering each other's columns): `availability_reconfirmed_at`, `deposit_amount_cents`, `deposit_confirmed_at`, `financing_choice` (`'own'`/`'help'`), `financing_income_range`, `financing_down_payment_cents`, `financing_desired_term_months`.
    - New `documents` table (1:many per offer — a deal can have multiple), `type` currently only `'financing_proof'` (`'service_agreement'` is the later PandaDoc addition, same table, no schema change needed for it). Backed by a new private Supabase Storage bucket (`documents`) — first real use of Storage in this project. No `storage.objects` RLS policies at all; uploads/reads go only through the `service_role` admin client server-side, same "no client-facing write path" convention as every other table here.
    - Financing is pure data capture, matching Core-Processes-v1.md exactly — never a credit pull (FCRA-regulated, needs a compliant vendor partnership later). `submitFinancingChoice` (`src/lib/deal-progress-actions.ts`) branches on the customer's choice: `'help'` just writes the three preference fields; `'own'` uploads the file directly via the Server Action (never a client-side Storage upload) to the `documents` bucket and inserts a `documents` row. Freely re-submittable — this isn't a state machine, a later submission just overwrites/adds via upsert, matching the change-of-mind case (a customer who resubmits from "help" to "own" correctly flips `financing_choice`).
    - Agent UI (`/internal/outreach`, only on offers at `status = 'customer_accepted'`): `ConfirmAvailabilityButton` (mirrors `MarkSoldButton` exactly) and `ConfirmDepositForm` (amount input + one button — sets both deposit fields together, since realistically both come from the same phone call with the dealer, not two separate steps). Read-only visibility into the customer's financing choice, including a short-lived (15m — tightened from an initial 1h after a deliberate check-in on how sensitive these files are) signed Storage URL to view their uploaded proof, minted fresh on every page load and never persisted anywhere.
    - Customer UI (`/account`, same gate): a "Congratulations — next steps" panel showing read-only availability/deposit status (agent-set) plus `FinancingCaptureForm` — a toggle between the two financing paths, conditionally rendering either the file input or the three preference fields.
    - Verified end-to-end in a real browser, sequentially as agent then customer (same cross-tab-session constraint as before): agent confirmed availability and entered/confirmed a $500 deposit — both read back correctly as read-only status text; customer submitted the "help" financing path (income range, down payment, desired term) — confirmed all three fields correct in the DB; customer then switched to "own" and uploaded a real file — confirmed in the DB that `financing_choice` flipped from `'help'` to `'own'`, a real `documents` row was created, and the actual file bytes in Storage matched byte-for-byte via a direct download-and-compare (not just checking upload succeeded); agent's view then showed the updated financing state and a working signed URL — fetched directly with `curl`, no auth, confirming it actually serves the real uploaded content, not just that a URL string was generated. Test data (2 auth users, 1 `customer_searches` row, 1 accepted `qualifying_offers` row, 1 `deal_progress` row, 1 `documents` row + its Storage object, 1 `agents` row) cleaned up after.
- **Not built yet:** the monthly low-movement sync tier (see above), outreach automation (deliberately manual for now), dealer-reply parsing, the `pending_refinement` → `searching` transition (24h post-payment auto-solidify), actually processing the Stripe refund for `guarantee_status = 'refunded'` rows (deliberately manual for now — see Day-30 assessment job above), the notification-sending system that actually acts on `customers.communication_frequency`/`communication_channel` (captured at signup now, nothing reads it yet), the $100 switch fee / 5-day grace period, the LEVR e-sign document (PandaDoc — blocked on a sandbox API key; the `documents` table/Storage bucket already support a `'service_agreement'` type, just unused so far), delivery coordination, fuller admin views — see Build order below for the sequence
  - **Re-verified against production, not localhost.** Supabase's Auth "Site URL" had been misconfigured to `localhost:3000` — meaning earlier local-only verification of signup confirmation/password reset could have been passing partly by accident (a redirect not on the allow-list can silently fall back to Site URL, which happened to equal what local testing wanted anyway). Fixed to `https://levrauto.com`. While in there: also found `NEXT_PUBLIC_SITE_URL` in Vercel Production was unreadable (masked by the CLI, same class of risk as the earlier MarketCheck key incident), so re-set it explicitly to `https://levrauto.com` and redeployed rather than trust the existing value. Re-ran both flows for real with the local dev server stopped (`lsof -i :3000` confirmed no listener) so there was no possible localhost fallback: a genuine signup via `https://levrauto.com/signup` → confirmation link → landed signed-in on `https://www.levrauto.com/account`; a genuine password reset via `https://levrauto.com/forgot-password` → reset link → new password set → logged in successfully on production. Both clean. Test account deleted after. (Caveat: link clicks were simulated via `admin.generateLink` with the production redirect explicitly matching what the app should send, same method used throughout this session, since there's no way to read a real inbox directly — this proves the `/auth/callback` and `/auth/reset-password` mechanisms work correctly on the live domain, but doesn't by itself prove what `NEXT_PUBLIC_SITE_URL` was before the explicit re-set — which is exactly why it was re-set rather than left to trust.)

## Pre-launch to-dos — don't forget these once there's real customer data

- **Preview deployments point at the same production Supabase project as live** (same DB, same auth users — no separate staging/test project exists yet). Fine for now since there's no real customer data, but this needs a proper split — a separate Supabase project for Preview, or branch-aware config — before real launch, so a test PR can never touch live customer data.
- **Stripe is in test mode everywhere** (`sk_test_...` / `whsec_...`, "LEVR Auto sandbox" account). No live keys configured anywhere — local, Preview, or Production. A real (test-mode) webhook endpoint is registered in the Stripe sandbox pointing at `https://levrauto.com/api/stripe/webhook` (`we_1U2cbg8FQdYEFttXePzCHvX8`, `checkout.session.completed` only), and `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` are set on both Vercel Production and Preview (Preview shares the endpoint/keys for now, consistent with Preview sharing the production Supabase project above). Before real launch: switch to live keys, and register a separate live-mode webhook endpoint (test-mode endpoints don't receive live events).

## Tech stack (decided)

- Next.js — app framework
- Supabase — Postgres DB, auth, storage
- Vercel — hosting
- MarketCheck API — dealer inventory data (live key exists, tested manually, not yet wired into the app)
- Stripe — payments
- ZeptoMail — transactional email (already set up via Zoho, same account as business email)
- Claude API — parsing freeform dealer reply emails into structured offers
- DocuSign or PandaDoc — e-signing

## Build order (do in this sequence)

1. ~~**Database schema first**~~ — **done**
2. ~~Auth (Supabase Auth)~~ — **done**
3. ~~Intake flow → writes to real DB~~ — **done**
4. ~~Payment (Stripe) tied to the 3 tiers: $699 / $899 / $999~~ — **done, test mode**
5. ~~MarketCheck integration — single make/model sync, manually triggered~~ — **done**; ~~demand-driven scheduling (nightly for active make/models, weekly for everything else already known)~~ — **done, via Vercel Cron**. Monthly low-movement tier deferred until real usage data exists.
6. ~~Outreach engine — manual work queue (not automation): `/internal/outreach` + offer-logging form~~ — **done**. Actual dealer contact (call/email automation) is still **mechanism deliberately unproven, deferred** — don't over-build automation here yet; keep this manual/lightweight until real-world dealer response patterns are observed
7. Inbound reply parsing (Claude API) → populates `qualifying_offers`
8. ~~Dashboard (customer-facing) — built into `/account`, sets `delivered_at` on first view~~ — **done**
9. ~~Change-request logic — accept/decline, the sold-before-response guarantee edge case, switching make/model, the Day-30 guarantee assessment job, and add-on negotiation~~ — **done**. The $100 switch fee/grace-period gating is a separate, still-unbuilt piece of this same build-order item (see "Not built yet" above)
10. ~~Financing/document flow — availability re-confirmation, deposit recording, financing capture~~ — **done**. The LEVR e-sign document (PandaDoc) is a separate, still-unbuilt piece of this same build-order item, blocked on a sandbox API key (see "Not built yet" above)
11. Delivery coordination
12. Minimal admin views

## Critical schema decisions already made — implement these correctly from the start

**`listings` vs `qualifying_offers` — these must be separate tables, not one.**
- `listings` = raw MarketCheck data (VIN, dealer, price, MSRP, trim, synced_at). Just sourced inventory, refreshed on a cadence.
- `qualifying_offers` = ONLY populated when a dealer actually responds to outreach with a real, itemized offer they're willing to honor. **An advertised listing price is never automatically a Qualifying Offer** — this is a hard business rule, not a UI nuance. Only rows in this table count toward the 30-day guarantee.

**`customer_searches` table doubles as the demand registry.** Drives MarketCheck sync cadence:
- Any make/model with an active row here → sync nightly
- Everything else → weekly, or monthly for consistently low-movement makes/models
- Rationale: most of the national inventory has zero active customers on any given day; pulling all of it nightly wastes API budget for no product benefit

**`assigned_agent_id` on every customer case, from day one** — even though right now it only ever points to Brett. This is what guarantees continuity (same agent stays with a customer) once there's ever a second person on the team. Cheap to build in now, annoying to retrofit later.

**VIN is the de-duplication key** for `listings` — upsert on VIN, never insert-always, or re-syncs create duplicate rows.

**Every MarketCheck sync call needs an explicit `car_type=new` filter** — testing showed the API defaults to mostly used inventory without it.

## The guarantee — exact rule (already decided, implement precisely)

- MSRP = Monroney label total (base + factory options + destination), excludes tax/title/doc/dealer add-ons
- Qualifying Offer = any dealer offer below that MSRP on a matching vehicle
- Assessed at Day 30 from payment
- **Sold-to-someone-else edge case:** customer gets a 24-hour response window from when a Qualifying Offer is delivered. No response within 24hrs + car sells → offer still counts (guarantee satisfied). Customer responds within 24hrs but car sells before it goes through anyway → doesn't count, LEVR keeps searching.
- Free search continues through Day 60 either way. After Day 60: $100 per ~30-day extension.

## Known open problems — don't design around false assumptions

**Outreach mechanism is unproven.** MarketCheck gives dealer phone + website, NOT email. ADF/XML lead delivery (the "real" scalable path) typically requires being a recognized lead vendor with the dealer's specific CRM system — a business relationship, not just an API call. Don't build heavy automation on an assumed email-outreach flow. This has been deliberately deferred as a business decision, not an oversight — revisit once real transactions are running.

**MarketCheck has no single-query nationwide radius.** Every tier caps a single search to 100-500 miles from one point. Genuine nationwide coverage requires running multiple searches anchored at different regional centers and stitching/deduping results (by VIN).

**MarketCheck pricing:** Free tier (500 calls/mo) currently in use for testing. Realistic production tier is ~$749/mo + an unspecified usage-based "data fee" — confirm exact numbers once their sales team replies with a real quote.

## Brand assets

- Logo files: `levr-auto-logo-final.png`, `levr-holdings-llc-logo-final.png` — both have real transparency, already in site header/footer/favicon
- Primary color: `#1746A2`
- Tagline: "Car buying, with the leverage on your side."

## Things NOT to build yet (explicitly deferred, don't scope-creep into these)

- Dealer subscription/lead-routing marketplace (Stage 4 — needs real transaction volume first)
- In-house review video content
- AI voice-calling for dealer outreach (real open question, needs legal review before any real use)
- Post-purchase mailer/giveaway program (has an unresolved sweepstakes-law question — needs attorney review before it's real)
- Lender referral, transporter marketplace revenue streams
